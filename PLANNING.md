# 上帝对话框：元系统规划

> 版本：v0.3（务实聚焦版）
> 日期：2026-08-04
> 状态：**regime-driver 实施中 —— M-1/M-2/M-3 已完成，安全监控与紧急停止已加固，进入 M-4 端到端试跑**。
> 历史：v0.1 完整元系统设计 → v0.2 聚焦 M0 稳健自主运行（已完成）→ v0.3 以 regime-driver 落地"上帝对话框"（M1+ 的务实实现）。

---

## 0. 为什么需要它

现有体系（opencode + goal-plugin + supervisor.py）已能"无人值守自主推进"，但它是**命令式的**：
你通过 CLI 启动、通过日志看结果。它缺少一个**唯一的、可对话的、能自我修改的控制面**：

- 想改策略要改文件，而不是"说一句"。
- 想让它"到点唤醒自己思考然后改脚本"做不到——它没有自我唤起的机制。
- 多个脚本/会话之间没有统一的仲裁与反循环保证。

**上帝对话框**就是把这个控制面做成一等公民：一个**能对话、能显示、能自我修改、能调度自省脚本**的元系统。

---

## 0.5 重新聚焦（v0.2 → v0.3）：M0 已完成，方向收敛为 regime-driver

> v0.3 更新：M0（稳健自主运行 MVP）已完成 ✅。方向收敛为 **regime-driver**（详见 `docs/DESIGN-regime-driver.md`）：
> 把 `workflow-regime/` 制度化流程编译成状态机，由固定代码机器人（L1）+ 审查者智能（L0）驱动**干净无插件**的开发者 opencode（L2），
> 像人类一样推进工作。M0 的 supervisor/oc-task 演进为 L1 骨架。

**M0 成果（已完成，HANDOVER §4）**：stall-watchdog 插件（thinking 超时 600s）+ 宿主 supervisor v2（T1/T2/L3/L4/L5 + 元分析确定性门）+ `oc-task.py` 任务接口 + git 管理。故障注入全矩阵验证通过（thinking 循环 / docker stop→T1+L4 / 模型故障→L3 / SSE 静默有界回退）。

**后续方向（regime-driver，v0.3 实施中）**：制度化流程外置执行，里程碑见 DESIGN-regime-driver §12。**M-1 worker 镜像已 ✅（`opencode-worker:1.18.11` 容器运行中，无插件 + serve --pure + reviewer 只读 agent）**；**M-2 L1 骨架已 ✅（正式工程包 `regime-driver`，src/ 布局 + cli→app→core/infra 分层，确定性门 + [WORK_DONE] 段协议 + 5 轮会话检查）**；**M-3 审查者接入已 ✅（`app/reviewer.py` 严格 JSON 判定 + 带反馈重试；skill 注入；节点语义化命名 + 后继限定 advance；判定回路闭环；任务控制文档读写）**；**安全监控与紧急停止已加固 ✅（独立监控线程 + 死循环检测 + 卡死 abort + 上报，实证 opencode abort 打断生成）**；当前进入 M-4 端到端试跑。

---

## 1. 定义（工程化翻译）

**上帝对话框 = 唯一的交互面 + 确定性调度器 + 可自我修改的自省回路。**

- 对外**只有一个对话窗口**，没有 CLI、没有裸配置参数。
- 所有操作（含改自身配置）都**通过对话**，在对话框内做**权限管理**后，交由智能体执行。
- 对话框对外提供**只读显示**：当前配置、脚本/流程清单、内部 session。这些显示**原则上不允许用户手动修改**。
- 内部 session 是"自动运行的 opencode"，**永远不阻塞对话框自身**。
- 对话框可以为**自己设计脚本**，脚本可**自动化、周期化执行**，甚至能**唤起对话框自身的决策/判断能力**，给某个 agent 上报运行/结构化信息；对话框被唤起后**反过来控制、启停、甚至改写脚本**。

---

## 2. 架构：三层 + 事件总线

```
┌──────────────────────────────────────────────────────────────┐
│ L0 上帝对话框 = 交互式元 agent（唯一交互面）                     │
│    · 对话 ↕ 权限门控工具（读/写邮箱、控制调度器）                │
│    · 只读显示：配置 / 脚本清单 / 内部 session                   │
│    · 事件驱动，永不被阻塞                                       │
│    · 可被"唤醒"（读总线事件）→ 决策 → 写命令                    │
└───────────────┬──────────────────────────────────────────────┘
                │  命令(异步, 无阻塞)      │  事件(异步, 单向)
┌───────────────▼────────────────────────▼────────────────────┐
│ L1 确定性调度器 = oc-meta daemon（控制平面）                   │
│    · 事件总线 / 邮箱（唯一通信通道）                            │
│    · 周期/定时脚本、T1/T2/T3 看门狗、deadline、代际号           │
│    · 强制"安全不变量"（对话框不可改）                           │
│    · 死锁检测 + 超时 + 人类升级                                 │
└───────────────┬──────────────────────────────────────────────┘
                │  派生 / 监督 / 收集
┌───────────────▼──────────────────────────────────────────────┐
│ L2 Workers = opencode 会话 / agent（执行，永远不阻塞对话框）    │
│    · 只向总线发事件、从总线收命令                              │
└──────────────────────────────────────────────────────────────┘
```

### 关键不变量
1. **L0 永不阻塞**：任何脚本/会话/agent 调用都异步 + 超时；阻塞的活交给后台，完成后总线通知。
2. **L1 是唯一调度者**：只有 L1 能启停/扩容/改写脚本，L0 通过命令间接实现。
3. **L2 是纯执行者**：不持有决策权，只能报告与请求。

---

## 3. 反循环五原则（核心）

解决"互唤起的自锁/死循环/循环依赖/干扰"：

### 原则① 打破直接调用环——事件总线，禁止互相调用
- 脚本**绝不直接调用**对话框；对话框**绝不直接调用**脚本。
- 二者只通过**持久化事件总线/邮箱**通信（异步、单向、无阻塞）。
- 调度器消费命令去启停/改写脚本。**直接调用栈被拆成两条异步边，环即断。**

### 原则② 对话框永不被阻塞（内核不变量）
- 对话框是内核，脚本是进程，事件循环是调度器。
- 该阻塞的活交给后台 worker，完成后总线通知。

### 原则③ 分层 + 安全不变量不可改写
- **可改写**：工作流脚本、`policy.json`（对话框/自省可改）。
- **不可改写（宪法层）**：看门狗频率、最大元深度、最大迭代数、deadline、权限边界——由 **L1 确定性调度器强制**，对话框无权改写。
- 否则 AI 能"关掉自己的监狱"，直接失控。这是防递归失控的**燃料限制**。

### 原则④ 自省是"有界、快照、独立子进程"
- 自我反思 = 独立、有界、超时的子会话，读当前状态**快照**，产出**建议文件**。
- 建议由**确定性门**（对照安全不变量）评估后，做**有界**的、可审计的修改。
- 带**代际号 + 预算 + 超时**，绝不无限递归。

### 原则⑤ 每次等待都有超时 + 死锁检测 + 人类兜底
- 每个 await 都有超时。
- 调度器持有**等待图**，检测到环（脚本↔对话框互相等）就按优先级**抢占**打破。
- 无法自解的环 → **L5 升级到人类**（人类永远是可拉下的刹车）。

---

## 4. 核心机制

### 4.1 事件总线 / 邮箱
持久化、可审计、可重放。事件类型示例：
```
workflow.requested / workflow.started / workflow.checkpoint
workflow.stalled / workflow.corrected / workflow.completed / workflow.blocked
meta.self_requested / meta.decided / meta.policy_changed
shell.checkpoint / agent.report / error.timeout / error.deadlock
```
- 脚本 → 只写事件；对话框 → 读事件 + 写命令；调度器 → 消费命令。

### 4.2 代际号（generation）
- 每次"自省 → 改写脚本/策略"产生一个新代际。
- 代际号用于：检测陈旧决策、限制递归深度、回滚。

### 4.3 预算与超时
- 每个自省/自改带 token 预算、时间预算、最大元深度。
- 超预算/超深度 → 强制终止并上报。

### 4.4 死锁检测
- 调度器维护等待图（who waits for whom）。
- 检测到环 → 按优先级抢占（先杀低优先级等待者），打破后重放。

### 4.5 人类兜底
- 所有层级都有 `L5 escalate` 出口。
- 人类可随时：暂停、回滚代际、手动改写策略、强制停机。

---

## 5. 自我改善 / 元流程分析

- **元复盘**：运行后把 run-ledger 交给独立模型会话复盘 → 产出建议 → 护栏校验 → 写回策略（有界、可审计）。
- **全局分析**：跨运行聚合（卡死率、纠正命中率、费用、模型表现）→ 发现系统性问题的模型/任务。
- **元流程分析**：评估"任务控制文档/计划本身是否健全"→ 是否重规划/分解/升级，不只盯任务做没做。

---

## 6. 落地路线图（v0.2 重新聚焦：M0 优先）

> 原 §6 的四里程碑（M1 调度器内核 / M2 对话框接入 / M3 自省 / M4 全局分析）整体**推迟**，
> 作为上帝对话框阶段的路线图（见 `docs/DESIGN.md`）。当前执行 M0。

### 6.1 关键判断：thinking 超时守护，插件 vs Docker 外脚本（不是二选一）

调研结论（`docs/RESEARCH-thinking-timeout.md` 与源码核对）：
- opencode 自身只有**传输层超时**（`provider.options.timeout` 默认 300s 单请求上限、
  `chunkTimeout` 默认 30s 静默兜底、`headerTimeout`），**没有 thinking 语义级守护**。
- 插件可用定时器 + `client.session.abort()`（goal-plugin 自身就用 setTimeout），
  进程内可实现 thinking 看门狗；可靠性弱在"宿主挂它也挂"。
- **goal-plugin 与 supervisor.py 有同一盲区**：前者用 `latestHasThinkingTokens` 排除
  reasoning-only（goal-plugin.js:4539-4554）；后者的 T3 指纹把 reasoning 长度算进进展
  （supervisor.py:61-79）。二者都判不出"只思考不出活"。

分工决策：
| 能力 | 进程内插件（stall-watchdog） | Docker 外脚本（supervisor.py） |
|---|---|---|
| thinking 循环检测（仅 reasoning 增长） | ✅ 事件级实时 | ❌ 指纹盲区 |
| 静默挂起检测 | ✅ 秒级 | ✅ 分钟级 |
| 进程级健康 + L4 重启 | ❌ | ✅ |
| 模型回退 L3 / 人工 L5 / 期限 | ❌ | ✅ |
| 独立性（宿主崩溃仍兜底） | ❌ | ✅ |

→ **M0 两者都做，分工明确**：插件负责"测得到"（thinking 超时实时检测+abort），
外部脚本负责"兜得住"（进程健康/重启/回退/期限/升级）。

### 6.2 M0 稳健自主运行 MVP（本次）

> 进度：① ✅ ② ✅ ③ ✅（`oc-task.py` 取代 oc-run 为任务接口）→ ④ ✅（thinking 全循环 / docker stop→T1+L4 / 模型故障→L3 / SSE 静默有界回退）→ ⑤ ✅（代码审查硬化 + git + 文档）。
> 元分析已并入 supervisor v2（智能判定 + 确定性门，见 DESIGN §6.4 / HANDOVER §4.8）。
> **任务接管接口**：`ops/oc-task.py`（submit/list/status/stop/logs/clean + 可选 web 状态页）；每任务独立进程 + 注册表，无 systemd 污染（见 HANDOVER §4.9）。git 已初始化（`.gitignore` 排除密钥/运行态）。

1. **stall-watchdog 插件**（进程内，新增，`ops/stall-watchdog.js`）：
   - 事件级订阅 `message.part.updated` / `session.status` / `session.idle`；
   - 跟踪每个活跃会话"最近一次 text/tool 变化"时间戳；
   - 判定 ①：busy 且仅 reasoning 更新持续 > `thinkingStallSec`（如 90s）→ 判 thinking 卡死 → `abort` 该轮（触发 goal-plugin idle 恢复）；
   - 判定 ②：busy 且完全无任何事件 > `turnStallSec` → `abort`；
   - 原则：只观测 + abort，不注入提示、不改目标；阈值全部可配；事件写 ledger。
   - **已实现细节**：part 类型靠 `message.part.updated` 学、流式增量走 `message.part.delta` 分类；
     abort 后 goal-plugin 会暂停（把 abort 当用户中断），故 watchdog 主动 `/goal resume` 恢复；
     时间窗连续计数（45min 内 >3 次 → `watchdog_gave_up`，abort 停流留暂停给 supervisor）。
2. **supervisor.py 加固**：
   - 改为**宿主侧**运行（`setsid`/systemd），持 docker 控制权 → L4 restart 真正生效；
   - T3 判定改为"忽略 reasoning 增长"（或整体移交给插件，脚本只管 T1/T2/期限）；
   - `policy.json` 显式加入物理兜底 `timeout`/`chunkTimeout` 建议值。
3. **单入口 `oc-run`**：goal + 策略 + 期限 + 账本一条命令；跑完打印摘要。
4. **故障注入演练**（4 类，全部需通过）：
   - 注入 thinking-only 循环（长时间思考模型 / 伪 provider）→ 插件 detect+abort+恢复；
   - 注入 SSE 静默断流 → chunkTimeout / 插件 ② 兜底；
   - 注入 worker 崩溃（docker stop）→ T1 + L4 重启；
   - 注入模型故障 → L3 回退。
5. **文档**：`DESIGN.md` 补研究修正（插件可定时器；盲区是设计选择+指纹盲区）；
   `HANDOVER.md` 更新到 M0 状态。

### 6.3 M1+ 上帝对话框（推迟）

- 事件总线/邮箱、代际号、安全不变量、死锁检测、L0 对话界面、自省/自我改善
  → 待 M0 稳定后，依据真实运行数据再启动（回归 §2-§5 设计）。
- run-ledger 持续积累，作为未来自我改善/全局分析的数据源。

---

## 7. 与现有成果的衔接

| 现有资产 | 在新体系中的角色 |
|---|---|
| `opencode-autopilot` 容器 | L2 worker（执行面） |
| `supervisor.py` + `policy.json` | 演化为 L1 调度器的前身（其中的看门狗/T1/T2/T3/纠正阶梯可直接复用） |
| `run-ledger.jsonl` | 事件总线/邮箱的初始数据源 |
| 3 个 skill + reviewer agent | L0 的策略/评审工具库 |
| `DESIGN.md` | 并入 `docs/`，作为设计依据 |

---

## 8. 待决问题（需你拍板；M0 已基本收官）

1. **真实工程任务试点**：通过 `oc-task.py submit` 接你的第一个工程任务（接口已就绪）。
2. **skills/agents 归属**：留在全局（全环境生效）还是迁入工作区（仅项目生效）——不阻塞。
3. **周期 code-review 是否并入 supervisor**（v1 的 do_review，v2 未带）。
4. ~~L0 对话载体 / supervisor 常驻方式~~ → 已用 oc-task 每任务进程模型解决（无 daemon 污染）；L0 上帝对话框推迟至 M1+。