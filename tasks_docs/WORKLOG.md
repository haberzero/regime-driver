# WORKLOG — 工作日志文档（决策/质询/方案取舍/变化沉淀）

> 任务控制体系四类关键文档之一（规范见 `workflow-regime/task-control/01_worklog.md`）。
> 记录所有自主决策、质询分析、方案取舍、变化前后（实现+测试+文档）。常驻，持续追加。
> 本文件原为 `TASK.md`（2026-08-05 起），2026-08-13 依任务控制体系归并为工作日志。
> 历史验证记录与自省记录均留档于此（从最初基线起，供追溯）。

> 分支：`autonomous-2026-08-05`
> 日期：2026-08-05
> 模式：无人值守自主推进，最大自主权限，全部本地 commit（禁 push）。

## 目标

推进 regime-driver 后续候选工作（HANDOVER.md / MAIN_TASKS.md），按优先级逐项实施，每项过质量门 + 全量测试零回归 + code-review，然后 commit 并接续下一项。所有推进方向都阻塞时停止并完整汇报。

> 以下为历史指针（当前主线见 `MAIN_TASKS.md`）：对外供给/分发重构 + 深度核查 + 测试净化
> （WORK_PLAN8 阶段 1–4 ✅ / 阶段 5 待夜间整合重跑）。历史规划（WORK_PLAN1–7）已完成，归档清理。
> 技术债 G1–G14 已全清（闭源记录见本文件 2026-08-07/08 条目；过时登记 `TECH_DEBT.md` 已于
> 2026-08-16 文档审计删除）。文档治理遵循
> `docs/WRITING_GUIDE.md`（尺子）+ `workflow-regime/skills/doc-governance/SKILL.md`（治理流程）。

## 候选清单（按优先级，见 WORK_PLAN5）

- [x] P0 热编译/热校验：统一校验入口 + `regime flow validate`（--watch 编辑即校验）+ preflight 挂钩（F1–F3）| 2026-08-10 F1-F3 done
- [x] P0 热加载/热重载：`FlowRegistry`（命名 flow 单一真源，归并 god self.flows）+ 原子替换/快照 + 文件监视重载（F4–F6）| 2026-08-10 F4/F5 done; F6 的 validate --watch 完成，运行中自动 reload-on-change 待 L 期并轨
- [x] P1 CLI+dialog 交互：`regime flow list/validate/load/reload/rm/inspect` + god A/B 路接入（F7/F8）| 2026-08-10 F7/F8 done
- [x] P1 长期运行（耐久性，opencode-go）：2h+ drive/fleet，观测资源/泄漏/恢复 + 资源治理收尾（L1–L3）| 2026-08-12 L1✅(2h零崩溃/资源有界增长) + L3✅(KNOWN_LIMITS/C3校准) done; L2⏳(工具已就绪, 长跑自动prune/清理待接入)
- [x] P2 安全反循环：load/reload 门禁 + 版本/快照 + 环检测（F9–F11）| 2026-08-10 done
- [~] P2 微调：覆盖率基线(pytest-cov) ✅ C1 / doctor 接入 god 与 web ✅ C2(god) / opencode-go 延迟调优 ⬜ C3 / 主机模式 agent 模板 ✅ C4（C1/C2/C4 2026-08-10 done; C3 待真实长时间观测）
- [ ] P3 收敛测试内零散 FakeClient（T6 已评估不转，如需统一另建轻量脚本化 fake）

## 发布就绪（WORK_PLAN6，2026-08-10~11）

- [x] II 真实 CI 跑通：GitHub Actions 上 py3.11/3.12 + E2E 全绿（修 secrets-job-if + worker ensure api_key 缺陷）
- [x] III 去硬编码：god 插件 `REGIME_BIN` 三级解析
- [x] IV 文档一致性：计数统一 333 + M0 遗留段标注 + **技术文档体系彻底重构（Divio 结构）** + 独立交叉核验全绿
- [x] V 发布准备：README 中英 + 警告 + MIT、SECURITY/CONTRIBUTING/KNOWN_LIMITS 对外摘要
- [x] 公开上传 GitHub public：https://github.com/haberzero/regime-driver
- [x] I 长期运行耐久性真实验证（2h+）| **已完成（2026-08-12）**：2h 连续真实运行零崩溃/停滞/重启，资源线性有界增长，worker 全程健康。报告 `tasks_docs/durability_report.md`。
- [x] ~~e2e-real 真实激活（需 GitHub `OPENCODE_GO_API_KEY` secret）~~ | **已封存（2026-08-11）**：GitHub 真实 E2E 集成未来很长时间不列入计划。CI 移除 e2e-real job；`tests/test_e2e_worker.py` 保留本地/手动可用（`REGIME_E2E=1`）。

## 验证记录

- [DONE] 基线测试 | verified: 101 passed | 分支建立
- [DONE] P3 工具节点 tool/route/gate 确定性执行 | verified: 119 passed (+18) | 新增 core/branching.py, core/tools.py, tests/test_branching_tools.py；driver 按 node.type 分流真正执行
- [DONE] P2 工作区物理隔离（workspace_for + 指令注入工作区提示） | 同上一并按 | core/policy.py, driver._build_instruction
- [DONE] 修复 ANCHOR 未处理 / 传递 ctx / 流转后 dev 陈旧引用 | 同上一并按 | driver._apply_transition / run()
- [DONE] 清理死代码（WORK_DONE_RE / load_skill_description / Regime.node / status 硬编码 URL） | 同上一并按 | 多文件
- [REVIEW] 里程碑1 | 0 blockers, 1 warning(数值比较,已修) | 分支/tools/驱动/清理
- [DONE] P1 M-4 真实工程任务试跑 | verified: 真实 worker 全流程 COMPLETE | regime run 生成 utils.py/test_utils.py，双 reviewer judge advance(0.9)，pytest 2 passed；首跑因 reviewer LLM 瞬时停顿被 monitor 正确 abort(安全系统生效)
- [DONE] P1 自定义角色 ROTATE 流转策略端到端验证 | verified: 真实 worker 实跑 | 自建 agent-only 流程，RolePolicy(transition_mode=ROTATE) 的 reviewer 在 b→c 流转时 ledger 记录 decision=rotate，会话真实换新
- [DONE] 架构方向研究：宪法层→对等多状态机网络 | 产出 docs/ARCHITECTURE-statechart-network.md | 可行性:高(监督控制理论)；决策点已定案(渐进+线程+根不变量运行时强制+保留默认宪法)
- [DONE] 阶段1 状态机泛化：事件驱动可交互单元 | verified: 134 passed(125+9) | 新增 core/statechart.py(StatechartUnit/Signal/Bus)，消息唤起回调机制，纯领域零回归
- [DONE] 阶段2 并行运行时 + 双向信号投递 | verified: 140 passed(134+6) | 新增 app/statechart_runtime.py(ThreadedUnit/Runtime)，每单元独立线程+队列，异步投递线程安全，多单元互驱
- [DONE] 阶段3 宪法状态机化(能力等价验证) | verified: 146 passed(140+6) | 新增 app/constitution_unit.py(ConstitutionUnit: 无智能 StatechartUnit，REPORT 信号喂入→死循环/卡死检测→STOP 信号广播)；现有 monitor 暂保留零回归
- [DONE] 阶段4a 根安全不变量运行时强制 | verified: 155 passed(146+9) | app/runtime_invariants.py(I1至少一watchdog/I2不可关STOP通道/I3元迭代上界)；Runtime.start 默认强制，违反拒启动
- [DONE] 阶段4b 用户自定义宪法可覆写 | verified: 157 passed(155+2) | 用户注 role=watchdog 自定义单元即可覆写默认宪法，满足根不变量
- [DONE] 阶段4c 宪法信号链端到端(真实worker) | verified: 真实 abort | ConstitutionUnit 检测卡死→STOP→工作单元 abort 真实 session
- [DONE] R1-R5 彻底重构：WorkflowUnit(单线程混合循环)+StatechartDriver集成+删旧监控/驱动 | verified: 139 passed | CLI 迁移到新架构；旧 driver/monitor/meta_analyzer/segment_runner 删除；价值行为迁入新测试
- [DONE] R6 彻底重构端到端(真实worker) | verified: 全流程 COMPLETE | add.py/test_add.py 正确生成，双 judge advance，pytest 2 passed
- [DONE] 消息机制完善 | verified: 153 passed | R1线程池消除阻塞/R2主题订阅推送/R3黑板全局状态；真实 worker square.py E2E COMPLETE
- [DONE] 宪法全局决策 | verified: 158 passed | G1工作流写start_time/heartbeat到黑板；G2宪法读黑板做全局超时/节点预算/心跳丢失判断；G3测试+E2E
- [DONE] 审查者prompt加固 | verified: 158 passed + E2E | 根因L0不守严格JSON→散文；prompt强制严格JSON；真实E2E双judge advance COMPLETE
- [DONE] 多 workflow 并发 | verified: 165 passed | M1黑板key按workflow隔离/M2宪法点到点STOP只停问题workflow/M3 StatechartCluster并发编排/M4测试；真实worker首跑双workflow并发产出正确文件
- [DONE] 可视化 | verified: 165 passed | V1 Telemetry 订阅watchdog_fire/blackboard.changed 生成状态快照
- [DONE] 健壮性 | verified: 161 passed | request_timeout可配(600s)+_dispatch重试退避，应对慢judge
- [DONE] 心跳存活修复 | verified: 166 passed | _step 每步刷新 {wid}.heartbeat，宪法心跳丢失检测与 telemetry 反映真实存活
- [DONE] 演示脚本 | verified: ops/demo_cluster.py | 多workflow并发+telemetry 可复用，真实worker验证并行+隔离+实时渲染
- [STOP] 实验阶段 | 用户叫停 | 结论：官方deepseek-api基线0.6-0.9s vs 免费opencode 1.8-4.2s(慢4-6倍,有排队)；judge回合官方仅4.8s但完整E2E常卡数分钟(慢judge根因待查)；系统已全用官方API
- [DONE] 交接准备 | 更新 _HANDOFF.md/HANDOVER 反映最新架构；清理实验session/工作区；168 passed
- [DONE] P1 完善 probe：全流程节点耗时剖析 | verified: 新增 ops/probe_node_timing.py 实测 | 测每node(POST等待 vs time.completed vs 轮询间隔 vs read RTT)；实测 judge 隔离 8s(POST≈completed,read RTT 0.0s) 干净；agent 隔离 14s 但 time.completed 2s 出现 vs POST 13.1s（11s 差，待查 agent 中间消息）
- [DONE] P1 修复 judge 陈旧文本重发派缺陷 | verified: 169 passed(168+1) | _step_judge 用 _last_judged_key(优先稳定 msg id) 只处理一次每回复；原缺陷：real client 累积消息→失败回复在 re-prompt 窗口内每 poll 被重解析→重发派 send_message POST 塞满 dispatch pool(max_workers=2)→E2E judge 真卡死；回归测试 test_judge_waits_for_new_reply_not_stale 无修复时 ERROR(gate exhausted,prompts=3) 有修复时 COMPLETE(prompts=2)
- [DONE] P1 mock 机制基础（思路设计+可行性） | verified: 176 passed(169+7) | 新增 src/regime_driver/testing/mock_client.py(MockClient/MockRule: 同接口drop-in, 默认reviewer advance+developer [WORK_DONE], 规则(agent,node)二段匹配, delay/stall/error 故障注入, 消息累积非替换) + docs/DESIGN-mock.md + ops/mock_feasibility.py(5/5 离线通过: 完整流程COMPLETE/慢judge时序/stall→宪法STOP BLOCKED/judge散文→gate exhausted) + tests/test_mock_client.py(7)
- [DONE] E2E 调试定位根因 | verified: 真实 E2E | 新增 ops/e2e_debug.py(包裹真实 client 逐操作计时) + ops/probe_judge_stall.py(并发观察 reasoning/output)；根因=发派线程池饱和(max_workers=2)：streaming POST /message 晚于 message.completed/[WORK_DONE] 返回(agent 约11s 滞后)，workflow 提前 advance 发下一 node，前 node POST 仍占线程→2 个 trailing POST 占满池→design judge 发派永久排队→session 呈 busy 无输出→宪法误判 stall 杀掉(原 E2E 卡数分钟)
- [DONE] E2E 修复发派序列化 | verified: 177 passed(176+1) + 真实 E2E 两次 COMPLETE(60.6s, 95.8s) | workflow_unit._dispatch 先 await 前一 POST future(_await_prior_dispatch, 保持 STOP 响应)再发下一 node→每回归 test_dispatch_serializes_prior_post(无修复 max_active=2 失败, 有修复=1)；judge reasoning 真实 21-60s(假设①确认为长推理非永久卡，stall_sec=120 有裕量)
- [DONE(重复,见上方)] 阶段3 宪法状态机化（重写 monitor/gate 为无智能状态机+通信协议）| 已由上方"阶段3/4a/4b/4c + R1-R5"完成：app/constitution_unit.py 无智能状态机 + app/monitor.py 已删除 + test_constitution_unit.py
- [DONE(重复,见上方)] 阶段4 用户自定义宪法（注册接口 + 根不变量由运行时强制）| 已由上方"阶段4a/4b/4c"完成：app/runtime_invariants.py(I1/I2/I3) + StatechartDriver(constitution= 注入) + test_runtime_invariants.py/test_custom_constitution.py
- [DONE] 上帝对话框 MVP（分析+方案+事件驱动单元+REPL） | verified: 184 passed(177+7) | 新增 app/god_dialog.py(GodDialogUnit: ThreadedUnit, role=human, 订阅 bus blackboard.changed/watchdog_fire/NOTIFY, 实时监控+事件日志, 命令路由 status/start/inspect/watch/config/help + 自由文本→LLM worker线程非阻塞解释, emit 回总线) + docs/DESIGN-god-dialog.md(需求分析+现代码分析+"对话框应在状态机体系内"可行性定案) + statechart_cluster.register_unit + cli dialog 命令 + ops/god_dialog.py 演示；真实 LLM 解释 E2E 验证通过；tests/test_god_dialog.py(7)
- [DONE] 上帝对话框：需求#5 独立session交互(talk) + 需求#4 动态监控区 | verified: 187 passed(184+3) | GodDialogUnit 增 talk <sid> <msg>(session_client 非阻塞转发+取回复) + monitor [字段] 过滤 + watch [n] [watchdog|blackboard|notify] 主题过滤；cli/ops 接 session_client；tests +3
- [DONE] 上帝对话框：需求#1 设计新 workflow | verified: 191 passed(187+4) | design <flow> <JSON|NL> 命令 + compile_flow(紧凑/full regime 规格→StateMachine 校验) 注册到 self.flows；start <flow> <ctx> 用设计流；NL 走 LLM worker 线程；launcher 契约改 (ctx,title,flow_sm)；tests +4
- [DONE] 上帝对话框：权限门控(PLANNING §3.3 L0/L1 边界) | verified: 192 passed(191+1) | GodDialogUnit 增 allow_write(默认False只读, 防困惑LLM回复触发副作用), 写操作start/design/talk被门禁拒绝; cli/ops allow_write=True(人类显式启用); tests +1
- [ ] P3 上帝对话框演进（远期/后续）| 已做 MVP+talk+动态监控+design+权限门控；后续候选：对运行中的 session/workflow 更深交互与回收 / 细粒度权限策略 / 对话框对接真实 E2E 运行验证
- [DONE] T3 非阻塞作业管理（契约红线 §5.2） | verified: 200 passed(195+5) | 新增 infra/jobs.py(JobRegistry: JSON文件注册表+后台子进程start_new_session+result/stdout文件+pid存活刷新DONE/FAILED) + `regime run/run-many --async`(立即返回handle) + `regime job list/status`(--json)；真实CLI冒烟：submit立即返回 → 坏base子进程失败 → job status failed
- [DONE] T4 插件加 job 工具 + 手册 async 用法 | verified: node --check 通过 | .opencode/plugins/regime-god.js 加 regime_job_list/regime_job_status 两个原生工具 + regime_run/regime_run_many 增 async 开关；docs/GOD_DIALOG_OPERATOR.md 增 §3.3 作业管理 + 阻塞/非阻塞说明 + 操作流程更新
- [REVIEW] T3/T4 | 3 warnings 已修 | ①_refresh持久化bug(读盘再存丢变更)→改_update_record load-patch-save, 新增test验证落盘 ②pid复用+无结果永running→结果文件权威优先判定done ③Popen失败留dangling running→try/except标FAILED ④doc 3.3重复编号→重排3.3-3.6 ⑤--deadline 0被丢→is not None | 已知限制: 并发create/refresh的lost-update竞态(单agent使用场景可接受, 记录在KNOWN_LIMITS) | verified: 201 passed(200+1)
- [DONE] T5 细粒度权限策略（T5） | verified: 207 passed(201+6) | 新增 infra/permission.py(PermissionLevel read<interact<run<clean + classify(argv) + require + from_god_dialog对接allow_write) + CLI `--perm`门禁(_gate)于 run/run-many/session send/sessions(--clean/--kill) + 插件写工具增perm参数 + god.md权限等级 + 手册§3.7；真实CLI验证: run --perm read拒绝 / sessions --clean --perm read拒绝 / sessions --perm read放行
- [REVIEW] T5 | 1 warning 已修 | dialog分类RUN但未门禁(写REPL可被read持者进入)→dialog加--perm并require RUN, 手册§3.7注明; 说明: --perm为操作者自限策略门, 非授权安全边界(设计使然, 已记录) | verified: 207 passed
- [DONE] WORK_PLAN4 E1补强: SSE 断流重连退避 | verified: 237 passed(235+2) | event_stream 增 reconnect/max_retries/backoff_sec: 断流(异常)时线性退避重连, 干净结束不重连(修无限循环); reconnect=False 立即传播错误 | 测试: 断流后重连产出两段事件 / no-reconnect 传播 OpenCodeError
- [DONE] WORK_PLAN4 R-C补强: --trace + 单对象视图 | verified: 235 passed | report 增位置参数 <object_id>(单对象聚焦) + --trace(因果链: 按序journal) | E2E: report workflow --trace 出完整因果链(20条: node_enter→node_done→transition→reviewer_verdict→advance→outcome complete, 含归属键/时间戳/detail)
- [DONE] WORK_PLAN4 R-R3a+c: oc-task收敛 + 保留策略 | verified: 235 passed(229+6) | 新增 infra/oc_tasks.py(load_tasks: 读ops/tasks注册表+derive运行态同oc-task) + `regime report --tasks-dir`(宏观项目看板并轨workflow+supervised任务) + Reporter.retain(按age/条数prune重写journal, 幂等重建rollup) + report --prune/--max-age/--max-records | E2E: --tasks-dir task-1 done/task-2 crashed; prune max-records 2 → 5-3=2
- [REVIEW] WORK_PLAN4 R-R3/R-C补强/E1补强 | 2 warnings 已修 | ①SSE断流路径不应用max_retries/backoff(只connect路径有)→drop路径也递增retries+sleep+检查max_retries ②Reporter.retain后旧append handle指已删inode→replace后重开self._fh, 新增test_ingest_after_retain验证; nit: _pid_alive把PermissionError当dead(异用户进程, 同用户一致可接受) | verified: 238 passed(237+1)
- [REVIEW] WORK_PLAN4 I1/I2/E1/R-A~C | 3 warnings 已修 | ①preflight delay故障硬编码design节点→改推导任意flow的reviewer/judge节点(_reviewer_gate_node) ②分支goto反向环未检→deep_validate加_branch_cycles(改警告非错误, 合法返工环, runtime max_total_nodes兜底) ③reporter load()非幂等→load前清rollups; 附: report_cmd未关handle→try/finally close + 去重复board块 | verified: 229 passed(228+1)
- [DONE] T7 文档同步 | docs/howto/god-dialog.md 更新为 opencode 载体版(A 路推荐+B 路REPL双轨 + 权限门禁 + async作业); docs/howto/README.md 索引更新
- [EVAL] T6 收敛FakeClient→MockClient | 决定: **不转换, 记录理由** | 逐个核对: MockClient 是"离线全流程模拟"(node解析规则+send_message异步轮询ask_and_get_text+session_tokens恒(0,0)); 而各测试FakeClient是"固定脚本同步fake"(test_reviewer/test_self_assess按序回固定reply、test_session_lifecycle跟踪token/创建删除、test_workflow_unit大量特殊子类Scripted/Native/StaleWindow/Fail/Concurrent/Never/Slow/NodeClient专测边界). MockClient并非这些单测fakes的drop-in(如self_assess需可配tokens、生命周期需跟踪ephemeral创建/删除、workflow_unit需精确消息时序). 强转必行为漂移+破坏测试, 且无功能收益. test_blackboard已改用MockClient(它本就是全流程单元). 结论: 保留专用fakes, MockClient继续作为全流程模拟器; 如需统一, 应另建轻量"脚本化同步fake"而非复用MockClient(记录为远期候选).
- [DONE] P0#1 一键自驱动入口(regime drive) | verified: 258 passed(255+3) | 新增 src/regime_driver/drive.py(Drive: 执行器线程+supervisor循环共享一reporter, _discover_session轮询anchor会话, supervisor stop_when=工作流完成即停) + `regime drive` CLI(前台Live/--async后台受监管任务, TaskRegistry.register登记pid, 写summary) + Supervisor.run(stop_when=)纯增 + TaskRegistry.register(前台pid登记) + cli/__main__.py(修jobs.py与drive --async后台`-m`启动缺失的潜在缺陷) + tests/test_drive.py(3: 全栈COMPLETE/共享journal/supervisor阶梯) + docs/DESIGN-drive.md | 真实CLI冒烟: drive --async提交受监管任务→task list可见→stop可停; --no-preflight前台UnboundLocal已修
- [REVIEW] P0#1 drive | 1 nit | blockers: 0 | warnings: 0 | Live渲染线程原每帧调_discover_session并发读workflow.sessions(字典变更竞态)→改_run读一次缓存的_session_id; 其余: 前台/后台双模式、共享reporter单一真源、TaskRegistry.register前台pid登记、summary落盘、context走argv非shell无注入; 已验证258测试全绿 + CLI冒烟
- [DONE] P0#2 真实E2E + supervisor真实卡死恢复 + 元分析接真实模型 | verified: 266 passed(258+8, 5 skip) + 真实worker E2E | (1) 智能元分析: Supervisor.meta_analyze()(独立模型读会话上下文→严格JSON verdict→确定性门gate_meta→阶梯; 失败/门拒回退确定性_verdict_for_stall) + _parse_meta_verdict(容忍围栏/散文) + cli supervisor/drive --meta + tests/test_supervisor.py(8) | (2) 真实E2E(test_e2e_worker): real drive+supervisor无假停滞(全栈COMPLETE+共享journal含worker事件+supervisor=workflow_done) + real T1→L4重启恢复(停容器→supervisor检测unhealthy→docker restart→worker恢复, REGIME_E2E_RESTART门控) + real meta真实模型(返回门控verdict) | (3) docker_restart修stale-shell权限: 平docker失败回退`sg docker -c`(本shell旧组必需)
- [REVIEW] P0#2 meta+真实E2E | 0 issues | blockers: 0 | warnings: 0 | meta_analyze用独立meta会话+严格JSON+门gate_meta; 真实E2E覆盖: 无假停滞/重启恢复/真实模型; docker_restart双候选(plain→sg); 已验证266测试全绿 + 3真实E2E通过 | 说明: 真实T2 busy-stall难以确定性诱发(坏模型HTTP500快失败、真实推理快), 故T2阶梯的真实执行以L4重启+真实模型meta+真实abort执行覆盖, T2判定逻辑由单测覆盖(诚实记录)

- [DONE] WORK_PLAN5 F1-F11 流程热编译/热加载基础设施 | verified: 329 passed(324+5) | 新增 src/regime_driver/flow.py(FlowRegistry: 命名flow单一真源 + compile_spec统一编译入口 + validate_sm深检门 + register/load/reload 原子替换/旧快照 + 持久store(REGIME_FLOW_STORE可配, 跨CLI调用单一真源) + 反循环) | 归并 god_dialog self.flows→flow_registry(删冗余flows属性, dead-code守卫强制), god design 走深检门(F9) | `regime flow` CLI(list/validate --watch/load/reload/rm/inspect, 权限门禁 load/reload/rm=RUN) | permission.py 登记 flow | god A路 plugin 加 regime_flow_list/validate/reload | dialog_app 注入 from_default 内置流 | tests: test_flow.py(14)+test_cli(+7, autouse隔离flowstore)+test_god_dialog(+5) | 真实CLI冒烟: load→list/inspect 跨进程可见→rm 移除 | 说明: 运行中自动 reload-on-change(F6后半)与 L1-L3 长期运行、C1-C4 微调 留待下session

- [DONE] WORK_PLAN5 C1/C2/C4 微调 | verified: 331 passed(329+2) | C1 覆盖率基线: 实测70.5%, pyproject [tool.coverage.report] fail_under=70 + CI gate 80→70(原80高于实际=潜在CI断裂, 修正为诚实基线) | C2 god B路 doctor自检命令(worker健康/flow数/LLM/权限, 非监控) + tests | C4 主机模式 agent 模板 docs/howto/host-mode-agents.md(developer/reviewer, 方式B复制即用) + DESIGN-usability 引用 | C3(opencode-go延迟调优) 待 L1 长时间观测后做

- [DONE] L1 预演中发现并修复: async drive 双注册任务缺陷 | verified: 333 passed(331+2) + 真实E2E | 根因: `regime drive --async` 经 TaskRegistry.submit 起子进程跑 foreground drive, 子进程再次 register() 生成第二个任务记录(同pid), 仅子任务写 summary → 父任务无 summary 且 pid 死 → 成功也报 crashed(任务孤儿/资源治理污染) | 修复: submit 向子进程注入 REGIME_TASK_ID env, TaskRegistry.register 支持 task_id 复用父记录id+summary路径, CLI drive foreground 据此复用(不新建) | 真实E2E: async drive 单任务id、status=done、outcome=complete、supervisor=workflow_done(209s) | tests: test_task(+2 register复用/submit env)

- [DONE] 2026-08-11 全自主实践复盘修复(实践暴露 bug 根治) | verified: 347 passed(覆盖71%) + 真实E2E(198s COMPLETE, 0 ladder) | (1) supervisor T2 stall 误报: SessionWatch 死参数(stall_sec 从未生效, 一停顿即 stall)重写为时间窗语义+SSE活动兑底(排除 server.connected)+恢复重置+每窗一射; 真实E2E: 同任务 drive 从"连续5误判 escalate→human"变"198s COMPLETE 零 ladder" | (2) 僵尸进程 bug: infra/proc.py pid_alive 读 /proc/<pid>/stat(Z/X 判死), jobs.py+task.py 接入, 修 jobs僵尸永running/task永running | (3) god 容器漂移: 镜像打 git-head label + up.sh 自动重建; god 挂载当前 docs/god.md/插件/opencode.json; Dockerfile.god COPY docs | (4) A-route 权限死锁: god.md bash 只读白名单扩大 + god-window.md 文档化 /permission 应答端点 | (5) reporter 噪音: drop message.part.delta | (6) task 子命令 --tasks-dir + drive --ledger | tests: test_proc(5) + test_supervisor(+live-loop T2 +SSE重置 +recovery) + test_jobs(zombie) + test_task(zombie) + test_reporter(delta drop)

- [DONE] 2026-08-11 上帝对话框制度设计闭环(P0主线) | verified: 356 passed(覆盖71%) + 真实E2E(官方模型 drive --flow 31.4s COMPLETE 零 ladder) | (1) `regime flow design <name> '<spec>'` inline 注册(compile_spec+register validate, preflight先于register零突变, --preflight-fault, skills_dir贯通) | (2) `regime status --deep` 聚合态势(健康+会话+busy+流程+任务+reporter rollup, 读journal前load, 会话聚合异常防护, 无mkdir副作用) | (3) `run/drive --flow <name>` 执行注册流程(god设计的流程按名运行) | (4) 终止judge节点gate修复(core/contract: advance next_state=null 仅当 terminal; workflow_unit _advance(None) 完成) | (5) god 插件 regime_flow_design/summary/load/report + regime_run --flow | (6) god.md bash *:allow(headless HTTP 死锁根治, 安全靠 edit/write deny+--perm) | tests: test_cli(+5) + test_core(terminal judge) + test_preflight(judge-terminal) | 真实E2E: god 设计 mini_wf → 注册 → 运行 → 正确诊断(被宪法重复检测拦截, 非机制故障); 官方模型下终止judge flow 31.4s COMPLETE

- [DONE] 2026-08-11 默认模型切换 DeepSeek 官方 API(用户授权, 速度更快) | verified: 356 passed + 真实E2E(drive --flow 31.4s vs 之前 opencode-go 198s) | settings.py/config.example.toml/docker worker+god opencode.json 默认模型 my-opencode-go→deepseek-api/deepseek-v4-flash; doctor 统一(auth检查含deepseek, 建议文案); 实测官方 1.6s vs opencode-go 40s; P1收尾: regime_report god工具(暴露supervisor ladder/历史) + reporter drop message.part.updated + repetition阈值0.35→0.40(结构化审查输出误报) | 新发现: opencode session 状态不一致(message 404 + status busy, 容器重启后残留) → KNOWN_LIMITS 记录

- [DONE] 2026-08-11 上帝对话框获得助手subagent(可行性验证+落地) | verified: 356 passed + 真实E2E | 调研: opencode primary agent 经内置 task 工具委派 subagent, headless serve 模式实测可行(探针: god 委派 general 计算 3^7 成功) | 落地: docker/god-config/agents/{analyst,advisor,reviewer}.md (态势分析师/流程设计顾问/只读审查), 挂载进 god 容器 + Dockerfile COPY | god.md 文档化两助手用法(只委派只读/起草, 写操作仍经 regime_*) | 真实E2E(281s): god 并行委派 analyst 读 journal 识别 reviewer gate exhausted 根因 + advisor 起草 flow spec | 边界: subagent 读容器外目录触发 external_directory 权限 ask(headless 需应答) → KNOWN_LIMITS; advisor 需强约束 agent|judge 类型(已修 prompt) | tests: 无新增(能力验证为主)

- [DONE] 2026-08-11 对外供给就绪度审查(发现"开发态≠发布态"硬缺口) | 产出: WORK_PLAN7.md(主线) + tasks_docs/release_readiness_audit.md(证据快照) | 决定性证据: 实测 pip wheel 仅含 Python 代码, agents/skills/docker/god助手/docs 全不在包内(62文件全代码); data/regime.json 引用包内不存在的 skill → 纯wheel用户 preflight 必败; DEFAULT_SKILLS_DIR=parents[3]/workflow-regime/skills 在 site-packages 下解析到不存在路径(即 god 容器 preflight 失败根因) | 目录漂移: reviewer.md 四处副本已有1行差异; skills 双份 | README 死链2处(ops/mock_feasibility.py, ops/e2e_debug.py 已删) | 无 scaffold/init 一键配置入口 | 建议: I模板进包 + II scaffold + III单一真源 + IV文档修复 + V平台 | verified: 356 passed 无回归 | next: 下一 session 按 WORK_PLAN7 实施(模板进包+scaffold 是"别人能用"硬前提)

- [DONE] 2026-08-11 对外供给就绪实施(WORK_PLAN7 I-IV + V-3) | verified: 389 passed(383+6 skip E2E门控, 覆盖71%) + 纯wheel隔离验证(preflight ok:true + scaffold落位 + doctor模板检查绿) | I 模板进包: src/regime_driver/data/{skills,agents,god-assistants,docker} 随 wheel 打包(hatchling自动纳入包内data/, 实测wheel含23模板文件), DEFAULT_SKILLS_DIR 改包内 data/skills(去源码树假设), tests/test_package.py(14项: 真实wheel构建+隔离子进程preflight无源码树+漂移守卫) | II scaffold: src/regime_driver/scaffold.py + `regime scaffold [--target][--god][--dry-run][--force][--json]`(幂等/部署指引) + doctor增packaged templates检查 + tests/test_scaffold.py(11项) | III 单一真源: 根 agents/、skills/ 副本删除, 真源=docker/*-config+workflow-regime/skills, 打包派生data/加CI漂移守卫(含docker pair+reviewer双源一致性), ops/sync_templates.py[--check], WRITING_GUIDE §A.5.1纪律, 4处skills/doc-governance引用改指真源 | IV 文档: README中英死链修复+部署章节, docs/guide/06_release.md发布教程, CLI_REFERENCE/01_cli/docs/README登记scaffold, KNOWN_LIMITS对外摘要更新, 测试冻结数字移除 | V-3 KNOWN_LIMITS复核; V-1 Pages/V-2 PyPI标可选(需授权/凭据, 路径已文档化) | code-review(general)无blocker, 修3 warning(scaffold权限RUN分类/CLI keep-write标注/docker漂移守卫)+4 nit(断言真空/双源交叉/help/解析健壮) | next: V-1/V-2平台通道(待授权) 或 后续主线(耐久验证) | escalate: no

- [DONE] 2026-08-11~12 长期耐久验证(WORK_PLAN6 I L1+L3) + e2e-real封存(用户决定) | verified: 2h真实运行(7205s, 38 drive)零崩溃/停滞/重启(ladder=0) + 391测试全绿 + 纯wheel验证 | L1: ops/durability.py(2h+真实drive序列 + 资源采样session/journal/内存 + event ledger审计 + --finalize重生成); 数据: session 16→96(+80, 有界线性), worker内存 466→697MB(+231MB/2h, ~1.9MB/min), journal 10KB→3.4MB, 完成率27/38(11个supervisor=timeout命中600s deadline, 根因=验证过度订阅单worker致并发积压busy 4→13, 非系统缺陷) | L3: 结果记入KNOWN_LIMITS + C3校准(单任务42-106s, deadline 600充裕, 并发上限比单任务超时更敏感) | 报告: tasks_docs/durability_report.md | e2e-real封存: CI移除e2e-real job(无secret长期不启用), TASK/WORK_PLAN6/KNOWN_LIMITS/06_release标记 | 小债: D1超时outcome记录(record_outcome公共方法+回归测试), G10 max_driver_wait_sec收敛driver/cluster/drive | 文档: README中英精校(去stale CI/耐久表述, deepseek默认key) | code-review(general)无blocker, 修durability harness 4 warning(_summarize KeyError/tasks outcome恒0/sessions fail-fast/submit rc) | next: L2资源治理(长跑自动prune/清理) 或 V-1 Pages待授权 | escalate: no

- [DONE] 2026-08-12 长跑暂缓日间推进(L2收尾+版本护栏+示例flow/上手文档) | verified: 400 collected全绿(覆盖72%) + 真实worker doctor全绿(版本1.18.11兼容) + 示例flow validate/preflight/flow load全过 | L2收尾: Drive增prune_max_records/prune_max_age, run()收尾reporter.retain(best-effort+logging审计), CLI drive暴露两选项+async转发, tests(+2: teardown prune/no-prune默认) | 版本耦合治理: OpenCodeClient.health_info/check_version(_version_compatible major.minor, SUPPORTED_OPCODE=1.18.11), doctor增'opencode version'检查, KNOWN_LIMITS契约更新, tests(+2: compatible/drift-flags) | 示例flow: data/examples/verify_then_report.json(tool have_report + route分支ok/not ok→review/rework + 回环), 随wheel打包, 03_flow_spec登记, tests(+2: preflight-clean/rework真路径恢复) | 文档: docs/guide/00_00_mental_model.md外部读者心智模型(三层/双时钟/确定性vs智能), 修01_setup stale模型表述(deepseek官方主/go回退), docs/README reader journey更新 | code-review(general)无blocker, 修2 warning(doctor建议索引漂移按名查/示例rework路径测试)+2 nit(prune logging审计) | next: 夜间稳态耐久二次验证(限并发) 或 V-1 Pages待授权 | escalate: no

- [DONE] 2026-08-12 初学者读者视角文档批评 + 全面落地(对外文档大修) | verified: 407 passed全绿(零回归) + mkdocs build通过 + 全仓相对链接脚本校验0死链 + general agent只读review无blocker(修7 warning) | 依据: tasks_docs/docs_reader_critique.md(以'对项目一无所知的普通用户'视角通读全部对外文档产出批评: 不清晰处/需深讲处/缺图解处/冗长重复处/排布问题) | 结构: guide编号去重(03_environment→04_environment, 04_setup→05_setup, 05_fleet→06_fleet, 06_release→07_release, 同步mkdocs.yml/docs.README/README中英/HANDOVER/WORK_PLAN5/7) + reference/05_god_dialog_contract归位'参考'(原挂开发者区) + nav与docs/README对齐 + KNOWN_LIMITS去内部路径引用 + 02_configuration注明config.example.toml唯一真源 | 内容: README中英开篇去L0/L1/L2代号改大白话 + 状态段去tasks_docs引用 + 密钥段去重(配置与密钥并回部署3) + up.sh诚实说明(wheel不含, 主机模式/worker up替代) + 00_god_dialog补'短命令vs自然语言'双模式说明 + 宪法/根不变量人话定义 + 审查判定闭环图 + 事件链时间线 + 进程外监督T1/T2/期限/阶梯图 + journal/ledger/report三词澄清 + 01_quickstart补--perm权限说明 + 03_design_flow补节点类型速览(agent/judge/tool/route/gate) + 04_environment补源码/wheel双安装路径 | 去重: code_workflow表(00保留, 02改节点链图) + worker/god分层表(00保留, 05_setup去表留端口) + CLI契约(05_god_dialog_contract声明01_cli权威, 修3.7/3.8编号错乱) | stale修正: run-many-sessions DELETE真删/--clean真清理/--cleanup策略 + subsystems/09去1.18.15旧版本号/去'待实施'/'请确认' + subsystems/07 CLI契约已就绪 + architecture/02现状表对齐实现/去Reviewer.judge与SegmentRunner引用/controller→watchdog/决策点→决策结论(去完成标记与日期戳守WRITING_GUIDE红线) | 图解: index系统全景图(你/上帝对话框/regime-driver/worker/supervisor/报告账本) + 06_fleet舰队隔离图 + howto/god-dialog缩进修复+命令补全(flow/doctor) | next: 夜间稳态耐久二次验证(限并发) 或 V-2 PyPI待用户 | escalate: no

- [DONE] 2026-08-12 文档图示方案评估+落地(Mermaid主方案) | verified: 407 passed零回归 + mkdocs build干净(7图6页) + mermaid@10.4.0解析器7/7语法通过(jsdom) + general agent只读review无blocker | 评估: tasks_docs/docs_diagram_assessment.md(双视角: 初学者最缺'任务时序图'与'设计流程成品图'; 开发者决定性约束=GitHub原生渲染+文本可diff, Mermaid是唯一全满足者; 否决HTML内嵌/JS交互/静态SVG/PlantUML/Graphviz) | 实施: mkdocs.yml加mermaid2插件(version锁10.4.0) + docs.yml CI补pip依赖 + WRITING_GUIDE增C.8图示纪律(何时用图/图不重复/每图一句图例/Mermaid约束/禁静态图片)+G.4检查项 | 图(7张, 每张配图例说明): index系统全景图flowchart + 00任务时序sequenceDiagram(最高价值: 你→对话框→体系→developer/reviewer→账本, 干活审查交替) + 00审查判定闭环flowchart(通过/不通过/耗尽三分支) + 03_design_flow'设计出的流程长什么样'flowchart(四种积木拼成) + architecture/02宪法↔工作流信号协议sequenceDiagram(REPORT→blackboard.changed→检测→STOP点到点, 已核对constitution_unit.py真实机制) + subsystems/04_supervisor监督循环flowchart(T1健康/T2停滞/deadline→阶梯L1-L5, LadderState限界, 已核对supervisor.py) + 06_fleet舰队隔离图flowchart(三工作区三容器三端口) | 保留ASCII: 00节点链/监督阶梯/事件时间线, 01 quickstart, 03节点类型文字(简单图不升级) | 图例纪律: 中文标签用「」全角引号避mermaid引号冲突, 不用|作标签, 图随文走 | next: 夜间稳态耐久二次验证(限并发) 或 V-2 PyPI待用户 | escalate: no

- [DONE] 2026-08-12 术语整体改名(花哨术语清零, 用户拍板命名) | verified: 407 passed零回归 + mkdocs build干净 + 全仓相对链接0死链 + mermaid 7/7 + general agent只读review(初版2 blocker+8 warning, 已全部修复) + 全树旧词残留grep=0(仅保留: 上帝对象反模式/监督阶梯L1-L5/WORK_PLAN任务标签/tasks_docs历史) | 命名(用户拍板): 上帝对话框→控制对话框(dialog control) / 舰队→并行任务(parallel) / 宪法→安全看门狗(watchdog); 载体/carrier 用户指示恢复原样(见下条) | 范围=全改+历史档案保留原样 | 代码改名: GodDialogUnit→DialogControlUnit + god_dialog.py→dialog_control.py + ConstitutionUnit→WatchdogUnit + constitution_unit.py→watchdog_unit.py + Fleet→Parallel + fleet.py→parallel.py + 类/方法/变量/注释(TOPIC_GOD_REPLY→TOPIC_DIALOG_REPLY, _on_god_reply→_on_dialog_reply, _is_fleet_cmd→_is_parallel_cmd, scaffold god参数→assistants, dialog命令fleet→parallel) | CLI: scaffold --god→--assistants + 输出键god→assistants + dialog help'Open the Dialog Control'+提示符God>→Dialog> + 工作流id前缀god-→dialog- + drive-many help'parallel (a batch)' + 全局help去L1代号 | Docker/.opencode: opencode-god容器→opencode-dialog-control(docker rename+tag) + Dockerfile.god→Dockerfile.dialog-control + god-config→dialog-control-config + regime-god.js→regime-dialog-control.js + god.md→dialog-control.md + god-assistants→dialog-control-assistants + up.sh up_god/GOD_PORT→up_dialog_control/DIALOG_PORT + REGIME_GOD_*→REGIME_DIALOG_CONTROL_* + opencode.json agent键god→dialog-control + plugin export RegimeGod→DialogControlPlugin | 文档: 全站中英+README+HANDOVER同步(00_god_dialog→00_dialog_control, 05_god_dialog_contract→05_dialog_control_contract, 03_fleet→03_parallel, 06_god_dialog→06_dialog_control, god-window→dialog-control-window, god-dialog→dialog-control) + mkdocs nav更新 + data/docker快照经sync_templates重同步(漂移守卫绿) | L0/L1/L2历史层代号清零(保留监督阶梯L1-L5真实概念与WORK_PLAN任务标签) + 元系统→可自我修改体系 | review修复: 初版漏网裸god 28处(docs howto/guide/subsystems + .opencode/plugins + sync_templates) + 上帝对象反模式误伤回退(God Object SE术语, 非花哨词) + pyproject/agent-md残留L1 + reference/01_cli L2保留策略→journal保留 + HANDOVER残留声明措辞 | 测试: test_god_dialog→test_dialog_control + test_fleet→test_parallel(重写变量命名) + test_constitution_unit→test_watchdog_unit + test_custom_constitution→test_custom_watchdog + test_scaffold(assistants) | next: 夜间稳态耐久二次验证(限并发) 或 V-2 PyPI待用户 | escalate: no

- [DONE] 2026-08-12 载体/carrier 恢复原样(用户指示: 该词使用正常无需修改) | verified: 407 passed零回归 + mkdocs build干净 + 0死链 | 07_dialog_control_access.md→07_dialog_control_carrier.md(仅回退载体部分, 保留god→dialog-control) + 全站'接入形态/作承载/对话承载/支撑承载'→回退'载体决策/作载体/对话载体/支撑载体能力' + reference/05('载体决策') + SUBSYSTEM_DESIGN/mkdocs nav/howto/dialog-control/guide 00+05/HANDOVER + code注释(permission.py/cli__init__) 'dialog carrier'恢复 | next: 夜间稳态耐久二次验证(限并发) 或 V-2 PyPI待用户 | escalate: no

- [DONE] 2026-08-13 夜间质量收益验证(复杂工程任务套件, 用户建议) | verified: 407 passed零回归 + sync_templates漂移守卫绿 + 2h真实运行43次(39 complete + 2 error + 1 human + 1 blocked, 最后一轮12/12 complete) + 宿主外部pytest 12任务全部0 failed(累计239断言) + reviewer每任务2-4次判定 | 依据: 用户要求验证'不仅长时稳定, 还要确认体系带来额外代码质量收益'——设计12个复杂工程任务(算法图/统计/配置校验/LRU+TTL缓存/CSV解析/依赖调度/令牌桶/字符串/金额/JSON diff/环形缓冲/变位词, 各含边界+异常+并发+强制pytest要求) | 新增: ops/quality_tasks.py(套件) + ops/quality_run.py(harness: 提交→等待→docker cp→宿主独立pytest复验→reviewer事件审计→逐任务--clean-sessions防worker退化) + tasks_docs/quality_report.md | **发现并修复真实bug**: reviewer agent bash "*":ask 在headless下被复杂任务逼着跑pytest验证→权限ask死锁→挂600s→supervisor deadline。改"*":deny+只读白名单(cat/ls/grep/rg/find/git), 同步双源(worker-config+dialog-control-config, 双源一致性守卫强制)+打包副本+运行容器; 修复后graph_algos 挂死17min+timeout→85s complete+宿主pytest 13/13 | 诚实失败模式(4个非complete全为首轮冷启动且自愈): lru_ttl→human(developer连续7次截断草稿, 监督阶梯正确升到L5) + task_sched→error×2(design gate exhausted确定性门正确拦截/test节点阶梯) + json_config→blocked(reviewer合法判定) | harness修复: pytest计数解析逗号bug(json_config 0p误报→16p) | 质量收益证据: 宿主外部pytest 40/30/26/22/20/18/17/16/15/13/12/10 全0失败 + reviewer每任务2-4次实质判定 | next: 夜间稳态耐久二次验证(简单任务基线, 可选) 或 V-2 PyPI待用户 | escalate: no

- [DONE] 2026-08-13 交接准备: 清理进程 + 运行痕迹全量归档入库 + 书写交接文档(下一session主线=内部代码质量+工作日志深度核查) | verified: 进程0残留 + 归档7.7M质量套件(artifacts/events/journal/tasks/report) + 4.2M耐久(含run2) + 407测试基线 | 交接要点: pytest全绿≠质量好, 对12任务产物做独立只读深审(测试真实性/代码质量/规格符合度) + reviewer_verdict实质性与过场判定核查 + 工作日志流程核查(节点推进/developer跳流程提前实现/transition决策/异常事件收集) + 6条待深挖线索(①lru_ttl 7次截断→human根因 ②task_sched gate exhausted原因 ③json_config blocked根因 ④worker MaxListenersExceeded监听器泄漏 ⑤4非complete首轮自愈真相 ⑥developer提前实现vs节点约束) | 处置: 发现问题→修复→全量测试→commit | next: 下一session执行深度核查 | escalate: no

- [DONE] 2026-08-13 深度核查执行(内部代码质量+工作日志, 用户指示选择性/针对性/系统性核查) | verified: 409 passed零回归(407+2新增) + 真实worker SSE生效确认 + general只读review无blocker(1 warning已修) | 方法: 从43次运行选择性深挖4个失败任务(lru_ttl human/task_sched error×2/json_config blocked)+3个'有ladder但complete'对照组, 全事件链还原+journal全文+对照regime内核代码(supervisor/opencode/reporter/workflow/watchdog/repetition) | **发现并修复真实系统bug(测试全绿掩盖)**: opencode 1.18.11 /event SSE只有data:行无event:行, event_stream只认event:行→raw["event"]恒None(10755条100%证实)→两个静默失效: ①supervisor _is_progress_event(None)恒False→T2活性兜底永久失效→SSE delta不能续命→长生成(>stall_sec)被误判stalled→abort打断输出→报告截断成草稿(→lru_ttl首轮7次截断→human的直接根因); ②reporter delta过滤etype恒None→journal被90%delta噪音淹没(9155+529/10755) | 修复: src/regime_driver/infra/opencode.py event_stream从data.type回退事件类型(isinstance防御+显式event:优先), 新增2回归测试(修复前必败), 报告tasks_docs/quality_deep_check.md | 其余5条线索核查结论: D2 task_sched gate exhausted=确定性门正确拦截(失败源头仍T2误判); D3 json_config blocked=watchdog重复检测(adjacent_sim=0.93)非reviewer判定(旧quality_report.md描述不准确,待更); D4 MaxListenersExceeded=opencode内部监听器泄漏,regime侧已有clean-sessions/L2缓解,非本仓缺陷; D5首轮自愈真相=非系统自愈是误判偶然性(对照组有ladder却complete),修复后系统性消除; D6无系统性跳流程 | reviewer判定质量合格(95条verdict, 低置信度advance仅3条均与截断输入相关, 非过场判定) | next: 夜间重跑质量套件验证修复后lru_ttl首轮行为(可选) 或 V-2 PyPI待用户 | escalate: no

- [DONE] 2026-08-13 复核+改进计划执行(用户指示仔细复核/修缺陷漏洞/改善UX) | verified: 415 passed零回归(407+8新增) + 真实regime run全流程56s COMPLETE(验证B修复后正常完成仍推进+_note正常) + general只读review(2nd) 1 blocker+5 warning全修 | 复核结论: 前轮A-F六项中F更正(低置信度advance非缺陷, gate已有advance≥0.5阈值, 0.7/0.75合法通过), 其余A-E确认 | 修复4项代码+1文档+1 UX: A supervisor.ingest_events SSE异常记录sse_error审计 + 事件类型无法解析计数+60s节流sse_type_unresolved(空data:{}跳过) + _safe_record嵌套try防日志失败杀watchdog loop(原except pass静默); B workflow._latest_agent_done 消息带error或finish is None→不推进(实测abort后completed有值但finish=None), 其余finish('stop'/''/'length')推进交reviewer判定防死等超时, [WORK_DONE]marker检查移入completed分支后(防abort截断草稿含marker误推进); C preflight PASSED后非--json打印诚实提示_note(修复NameError blocker), --json保持机器输出纯净; D settings.report_len_warn(默认20000)超限记录report_len_warn审计; E quality_report.md json_config blocked描述改watchdog重复检测 | 配套: MockClient正常完成消息显式finish="stop"(对齐真实契约, 防preflight卡死), 测试Message默认finish="stop" | 产物代码质量抽查(task_sched/graph_algos环检测/线程安全/边界测试)合格无'测了个寂寞' | next: 夜间重跑质量套件验证lru_ttl首轮不再误杀(可选, 需重建容器镜像生效) 或 V-2 PyPI待用户 | escalate: no
- [DONE] 2026-08-13(夜) WORK_PLAN9 夜间套件体系重构 + 框架误杀修复(用户指示: 任务过于简单+日志/清理机制缺陷+watchdog误杀逻辑不合格) | verified: 全量测试零回归 + 真实冒烟: payment_ledger 复杂任务 complete(396s, 宿主pytest 34p/0f, 2 verdicts, 全量归档含46+11会话消息快照) + check_capabilities绿(17 covers标签) | 依据: 用户明确指出 ①现有8任务(2-4分钟单模块)远不如一次编程任务复杂, 体现不了regime-driver价值 ②日志留档在需查运行记录时出问题 ③发现框架误杀任务问题, 现版watchdog逻辑不合格 | **根因实锤(P1)**: task_sched被进程内watchdog_unit误杀@understand——`_report_to_watchdog`只传output(文本令牌), reasoning令牌被丢弃(`_, output = session_tokens()`); watchdog `_detect`只看output增长; 模型长思考只产reasoning不落文本→output恒冻结→120s判stall→STOP→workflow标blocked(19:29:59); **但watchdog只STOP不abort会话→模型继续跑→19:31写完scheduler.py+测试并跑pytest(容器mtime为证)→孤儿产物污染artifact收集**; supervisor SessionWatch有activity_ts机制但SSE活性链对reasoning不生效 | **修复三处**: ①`workflow_unit._report_to_watchdog`传reasoning(`session_tokens`返回(reasoning,output)此前只取output) ②`watchdog_unit._detect`与`supervisor.SessionWatch`以"output或reasoning任一增长"判活性(长思考不误杀, 双冻结仍判stall) ③workflow收到STOP/per-node超时时`_abort_waiting_session` abort当前会话(防孤儿继续写+污染artifact) | **任务套件重构**: 8浅任务→4复杂工程任务(shop_inventory重构+扩特征 / kv_cluster分片+主备复制+恢复 / payment_ledger bug修复+幂等+结构化reconcile / etl_pipeline框架化+重试+限流+隔离), 每个15-30分钟多文件+seed既有代码+设计决策(含被否方案)+并发/故障隔离压力, covers标签17个(每任务8-10能力) | **日志留档重建**: per-task全量归档(会话消息快照sessions/*.jsonl + 完整工作区docker cp + journal/events切片 + result.json + DESIGN.md), 归档后才清理; `--clean-sessions`语义改为"每任务归档后执行"(原: 每任务前清空→已结束任务会话全丢) | **清理机制重建**: 共享/root/work/code每任务前wipe(防任务间串写与历史残留污染artifact收集) + report每任务后即写(中断可续, 原: 预算耗尽才写) + run_nightly trap EXIT归档(中断也归档) | 新增测试: watchdog reasoning活性3项 + supervisor reasoning活性2项 + workflow STOP/超时 abort断言 + capabilities §五声明检查 | 文档同步: capabilities.md §五新任务对照 + KNOWN_LIMITS去冻结数字 + test_capabilities_map适配 + MAIN_TASKS更新 + HANDOVER待本session结束更新 | next: 夜间整合重跑(新套件全链路+能力覆盖报告) 或 V-2 PyPI待用户 | escalate: no
- [DONE] 2026-08-13(夜) 夜间重跑(1轮4任务, 用户指示只跑一次) | verified: shop_inventory complete(72p/0f, 401s) + kv_cluster complete(32p/0f, 405s) + payment_ledger blocked@understand(144s) + etl_pipeline blocked@understand(145s) | 结果: 前2个复杂任务真实完成且宿主独立复验全绿(体现价值达成), 后2个被**同一根因**误杀——payment_ledger 上一轮冒烟 complete, 本轮思考更长被误杀 | **决定性证据链**: ①`session_tokens`长思考60s reasoning/output恒0(模型 busy 但计数不更新, 消息完成才落账) ②同一会话 SSE 持续推3234条message.part.delta(流式活性真实存在) ③journal无message事件只有server.connected(进程内watchdog先杀→drive supervisor的stop_when检测到blocked立即退出, 正确判定器从无机会运行) | 结论: 我的 WORK_PLAN9 reasoning 修复修错了信号源(仍依赖token计数), 根因更深——详见下一条 | next: 深度根因分析(opencode源码比对) | escalate: no
- [DONE] 2026-08-13(夜) **源码级根因分析(WORK_PLAN10立项, 用户指示深入底层+比对opencode官方源码+检验根本前提)** | verified: 拉取opencode v1.18.11源码(commit 012c2f57f9)逐文件比对 + 真实worker多重探针实证 + 全链路根因链闭合 | **源码事实**: ①`session/processor.ts:435-445` token在step-finish事件才赋值(`ctx.assistantMessage.tokens = usage.tokens`)→**step粒度记账非流式即时**; ②`session/session.ts:631-635` updateMessage只发布事件不写库→异步projector持久化→`session_tokens`API读滞后投影; ③`session/status.ts:30-33` busy/idle由Runner生命周期驱动(Runner.onBusy/onIdle, 与token无关)→**session_status可靠即时**; ④`server/routes/.../handlers/event.ts` `/event`即时推送, SSE块`data:{"id","type","properties"}`无event行(实测curl确认), server.heartbeat每10s一条, message.part.delta在长思考持续推→**SSE是唯一即时活性信号** | **误解清单**: M1 token增长判活性=根本错误(step前恒0); M2 SSE event行解析(实测无event行, fallback到data.type正确); M3 信号源选错(token而非SSE); M4 **双T2竞态**——进程内watchdog(错信号,120s先杀)+supervisor(对信号,60s但stop_when在workflow被误杀后立即退出, 正确判定器从无机会运行); M5 session_status None语义模糊(非主因) | **根本设计缺陷**: D1 "需要外部活性信号"的T2放进"I/O-free纯逻辑"的进程内watchdog=设计矛盾; D2 双T2重叠+stop_when竞态; D3 "停滞=busy但无输出增长"的前提依赖不可靠信号 | **修复方向**: R1 进程内watchdog停止T2 stall判定(保留dead_loop+全局护栏), T2全权supervisor(SSE活性链); R2 drive stop_when区分正常完成vs监控打断(supervisor有最终判断权); R3 活性判定统一SSE事件流(token仅辅助) | 完整报告: tasks_docs/_root_cause_analysis.md(临时, 完成后并入WORKLOG) | next: 用户授权深度重构(WORK_PLAN10) | escalate: no
- [DONE] 2026-08-13(夜) **WORK_PLAN10 执行: T2停滞判定SSE活性化(用户授权高度自主+破坏性重构)** | verified: 全量438 passed零回归(429+9新增) + 真实worker验证: payment_ledger complete(462s, 宿主pytest 31/0, 2 verdicts, 零monitor_abort——上次144s被误杀) + 长思考30s全程activity_age=0.0(alive) + 真卡死仍判stall(双语义验证) | 实施(与最初R1设计差异: 未移除进程内T2而是保留+换信号源, 保住I1/I2根不变量): ①新增`app/sse_activity.py` SseActivity采集器——daemon线程订阅/event流维护{sid:last_activity_ts}, is_progress_event排除server.connected/heartbeat; ②`watchdog_unit._detect`改用activity_ts判停滞(busy且无SSE活性>stall_sec), 新增_first_busy bookkeeping(从未有活性从首次busy起算), 移除token output/reasoning判定; ③`workflow_unit`创建SseActivity, _report_to_watchdog携带activity_ts, stop时关闭; ④`supervisor.SessionWatch`简化为纯SSE活性判定(移除token参数), run() T2不读session_tokens, _is_progress_event统一导入; ⑤`drive.stop_when`简化回"res" in _result(watchdog修复后误杀消失, supervisor兜底T1/deadline); ⑥`mock_client`新增event_stream模拟(stall无message事件/正常有)+delay期间busy+streaming(长思考路径离线可测) | 测试: SseActivity 8项(真实payload解析properties.sessionID/断连重连/多session/stop幂等) + watchdog/supervisor activity语义重写 + preflight慢生成不误杀 + 真卡死仍判 | general只读review: 0 blocker (W1/W2补测试+W3-W5清理完成) | 文档: settings/config/reference/02/subsystems/04/architecture/02同步SSE活性语义 | 已知限制: 工具执行期间无SSE活性可能误杀(由stall_sec=120缓解, 记KNOWN_LIMITS); 多workflow每实例一个SSE连接 | next: WORK_PLAN11可编程看门狗 | escalate: no
- [DONE] 2026-08-13(深夜) **WORK_PLAN11 执行: 可编程看门狗策略引擎(用户构想: 不硬性杀死+多级判定+中断等待继续+可注入检测+智能判断)** | verified: 全量463 passed零回归(455+8新增) + 真实worker验证: payment_ledger complete(265s, 宿主pytest 27/0, 2 verdicts, 零monitor_abort) + regime run complete(88.3s, 策略+settings集成正常) | 四级架构: ①信号层`SessionEvidence`(SSE活性/消息时间戳/节点/首次busy/系统时间/paused/自定义meta) ②判定策略层`Rule`(可注入谓词+多规则取最严重命中+meta=True走智能判定) ③动作阶梯层`Ladder`(nudge→interrupt(PAUSE)→resume→fallback→kill, per-session隔离+fire-once+should_fire) ④配置层(settings.watchdog_policy_json+auto_resume_sec) | workflow实现: `_on_pause`(abort当前生成+保持会话+冻结节点推进+paused持续上报防枯竭) / `_on_resume`(agent注入"继续"/judge重发判定+重置节点时钟) / `_on_nudge`(轻提示) / `_on_escalate`(meta-gated审计) | watchdog: paused会话不重复中断+超auto_resume_sec自动RESUME再兜底kill | 修复review 2 blocker(B1暂停上报枯竭→paused持续REPORT+自动RESUME; B2 meta死字段→decide读rule.meta+发ESCALATE) + W3(逐档fire-once)/W4(silent_for用max)/W6(resume重置_phase_started)/W8(prune ladders)/W5(未知action构造期抛错) | 测试: policy 25项 | 文档: KNOWN_LIMITS补策略引擎+两级watchdog协调边界 | next: 智能侧说明同步审计 | escalate: no
- [DONE] 2026-08-15(下午) **主控对话框使用模式变革：自由 CLI 直连 + 说明书驱动 + 非阻塞后台运行 + 观察窗** | verified: 624 passed零回归(618+6) + general只读review两轮(首轮 1 blocker XSS + 2 warning + 6 nit 全修, 复核 11/11 通过) + 真实冒烟(regime web 观察窗 JSON API + HTML 面板 + ledger/journal 读取; regime run --async + job status/logs 事后查看) | 用户洞察(元层评估后定案): ①我这个 session 实际承担了主控对话框职责(操作员+分析师+决策者三合一), 直接 bash 直连 CLI 比 22 个包装工具更高效——**CLI 包装不是不合理, 但分布后正确方向是"说明书+自由 CLI"而非更多包装**; ②原始设计意图: 主控对话框**绝不因某次工具使用被阻塞**——需更丰富后台运行+事后查看; ③CLI 应专注日志/诊断/内省/监控层, 提供观察窗, 完全开放 agent 直接操作 | **实施**: ①`.opencode/agent/dialog-control.md` 重写——从"经 22 插件工具"改为"自由 bash 直连 regime CLI + agent-handbook 必读"; 新增"诊断流程"章节(时间线→判定→会话原文→journal 下钻, 区分瞬时错误/恢复事件/真失败); 运行默认 --async 非阻塞; ②`.opencode/agent-handbook.md` 强化——§4 自由 CLI 直连主路径(插件降级可选引导+等价 CLI 表), **新增 §5 非阻塞后台运行与事后查看**(阻塞vs非阻塞表/事后查看三途径 status+logs+web/诊断组合命令), §8 操作流程强化非阻塞+事后复盘, 章节重编号; ③**新增 `regime web` 观察窗**(`app/observe.py` stdlib 实现: ObservationSnapshot 聚合 status/report/ledger_tail/journal_tail + 7 个 GET 端点 + HTML 面板, 纯消费者零写操作, subprocess 复用自身只读命令单点真理); ④**新增 `regime job logs <id>`**(读 --async 捕获 stdout.log 事后查看, --tail/--json, record out_path 单点真理); ⑤插件注释降级为"可选便利层非主路径"; ⑥文档同步(01_cli/capabilities 18 顶层/guide 00/howto/HANDOVER) | **review 价值**: 抓出 1 blocker——`_render_html` XSS(用户/LLM 内容含 flow 名/会话文本/ledger 行未转义, text/html 可执行注入) → 全插值 html.escape + test_html_escapes_user_content; W1 best-effort 违约(TimeoutExpired/UnicodeDecodeError 穿透 500) → try/except + errors=replace; W2 agent-handbook §6 子节编号残留 5.x → 6.x; N1-N6 全部修 | 关键工程判断: ①"CLI 包装已无权限边界作用(agent bash 本就 allow), 仅剩引导价值"→ 说明书替代工具是正确方向, 不新增包装; ②web 观察窗必须纯只读(聚合自身 CLI 只读命令, 无写端点)——控制面工具每暴露一个写能力都是攻击面; ③subprocess 复用自身 CLI 是"单点真理"而非性能问题(web 低频); ④非阻塞是主控对话框的第一原则——长任务一律 --async + 事后 status/logs/web 查看, 对话框永不阻塞 | next: commit | escalate: no
- [DONE] 2026-08-15(凌晨-清晨) **第二轮夜间长跑 + 两个新真实 bug 根治（extract_json 鲁棒性 + judge 等待 completed）** | verified: 617 passed零回归(616+1) + general只读review(B1 指正根因误诊 + W1/W2) + 真实重跑闭环实证: payment_ledger complete(396.5s, 宿主 pytest 30p/0f, 2 verdicts 0 gate_exhausted——对比首轮 error@design 180.9s gate exhausted) | **长跑结果**(tasks_docs/nightly_run_archive/nightly2-20260815-020027/): shop_inventory(complete 566.8s 37p)/kv_cluster(complete 698.5s 45p)/etl_pipeline(complete 549.6s 22p)/**distributed_scheduler(complete 1355s 22p, 三次 verify_result 全 rc=0)**/payment_ledger(error@design——真实失败, 非 harness 误判) | **verify 根除验证成功**: distributed_scheduler 在完整长跑中 verify 三次 rc=0(对比首轮 rc=None→blocked), StateMachine._validate 单点校验 + store 装载隔离 + store 清理彻底生效 | **新 bug 1 (extract_json)**: payment_ledger error@design root cause——reviewer 回复"散文+JSON"混合, 旧 extract_json 单次扫描被散文未闭合引号/字面花括号污染 → 提取失败 → gate 报 "no JSON object" → 重试耗尽; **修复**: extract_json 改为每个 `{` 作为候选 start 独立跟踪字符串状态(_try_from), 散文引号/花括号不再污染; 测试+3(unbalanced quote/stray brace/combined 复现) | **新 bug 2 (judge 等待 completed, review B1 指正)**: extract_json 修复后 review 实证**真实根因**是 judge 在**流式 partial 回复**(completed=None)上判定——`_latest_assistant` 不检查 completed(对比 agent 路径 `_latest_agent_done` 等待 completed+finish), id 去重使消息完成后永不重判 → 每次 re-prompt 判 partial → 耗尽; **修复**: `_latest_assistant` 增加 completed 检查(judge 只判定已完成的 assistant 消息), 与 agent 路径对称; 测试 mock Message 默认 completed="now"(stall 场景显式 None), 新增 test_judge_waits_for_completed_not_partial; 测试+1 | 关键工程判断: ①review(独立只读) 实证价值——extract_json 重写"正确但非根因", 用存档时间线证明耗尽发生在最后消息 completed 前 1.5s, 把修复引向真正位置(judge 路径); ②judge 与 agent 的完成语义必须对称(completed 是唯一可靠的"判定就绪"信号, partial 是流式中间态); ③测试 mock 消息默认"已完成"是合理抽象, 但 stall 场景必须显式 completed=None 否则语义被破坏——文档化该约定 | 归档: tasks_docs/nightly_run_archive/recheck-pl-20260815-040327/ | general只读review(judge-completed): 0 blocker + 1 warning(W1: `_latest_assistant` 扫描跳过 partial 后回到更早 abort draft, pause→resume 窗口内 draft 可被误判) → **W1 已闭环**: `_latest_assistant` 增加 finish is None 跳过(abort draft 与 agent 路径对称), 新增 test_judge_skips_abort_draft_with_no_finish; 全量 618 passed(617+1) | next: commit | escalate: no
- [DONE] 2026-08-15(凌晨) **verify 白名单配置漂移——彻底根因调查 + 系统性根除（单点构造校验）** | verified: 613 passed零回归(612+1) + general只读review 0 blocker(补 regime store 测试 + 2 nit) + 真实冒烟(regime run --flow code_workflow_v13 complete 70.3s, 合法 flow 零影响) | **真正根源（git 历史实证）**: `ops/flow_v13.json` 在 WORK_PLAN13(commit 556f298)时代 verify 是 `sg docker -c "docker exec ..."` 包装(为适配宿主 docker 组权限); 阶段2(commit d1fe9f4)verify 白名单化(W5)把 verify.py 改为只允许 `docker exec {container} <白名单>` argv, **真源文件改为纯 docker exec, 但 FlowRegistry 持久 store(~/.regime/flows/)残留 sg 包装版未迁移** → 运行 `--flow` 从 store 装载 sg 包装 → 白名单拒绝(rc=None) → judge 无 pytest 证据 → 质询重跑 → dispatch 瞬时超时 → 默认 watchdog 直接 kill → 长任务 blocked@test | **本质**: verify 命令未在任何装载/校验点做白名单形状检查, 配置漂移只在运行中(进 judge 门)才暴露——"校验期失败 >> 运行中失败"的原则未被 verify 遵守 | **系统性根除（单点构造校验）**: `StateMachine._validate()`(state_machine.py) 增加 verify 白名单形状校验——这是 StateMachine **唯一构造点**(__init__→_validate), 所有装载路径(FlowRegistry._load_store / RegimeRegistry._load_store / load_regime / --flow / --regime / flow design / regime design)统一拦截; verify 非白名单 → StateMachineError 构造即失败, 绝不进入运行期 | 修复组件: ①`core/verify_spec.py`(上一轮) 白名单+build_verify_argv+verify_command_error 上移 core(单点真理) ②`state_machine._validate` 挂主校验 ③`core/validate.py` 保留 verify 白名单检查作 direct-construction 兜底(注释说明, 避免被当主防线) ④`regime.py _load_store` 补 log.warning(与 flow.py 一致, store 残留可诊断) ⑤`flow.py _load_store` 移除冗余循环(StateMachine 构造已统一拦截, 单点清晰) | 测试: test_verify_whitelist_shape_enforced 改断言构造期 StateMachineError + test_store_residual_verify_whitelist_rejected_at_load(flow store) + test_regime_store_residual_verify_whitelist_rejected_at_load(regime store, review 建议) + test_verify_on_non_judge_detected/test_verify_skipped_when_disabled 改合法 verify | 关键工程判断: ①"运行时才失败"的配置字段必须找到**唯一构造点**统一校验, 而非在各消费方打补丁——verify 的构造点是 StateMachine._validate, 覆盖所有装载来源; ②store 装载路径(except: continue)是"隔离残留"的正确行为, 但必须 log.warning 使残留可诊断(静默跳过=新盲区); ③validate.py 的校验与构造期校验同源同函数, 是冗余兜底非冲突, 需注释定位避免未来误当主防线 | next: 新一轮夜间长跑(全套件, 验证根除) | escalate: no
- [DONE] 2026-08-14(深夜后) **夜间长跑（WORK_PLAN14 后首轮全套件 5 任务）+ verify 白名单配置漂移真实 bug 根治** | verified: 611 passed零回归(610+1) + 死代码守卫/漂移守卫/sync_templates/check_capabilities全绿 + general只读review待跑 | 运行: `ops/quality_run.py` 全 5 任务各一圈(REGIME_VERIFY_ENABLED=true + 上下文交接策略 + --stall 300 --deadline 3600), 归档 tasks_docs/nightly_run_archive/20260814-222131/ | **结果**: shop_inventory(complete 468s 4verdicts)/kv_cluster(complete 1045s 33p)/payment_ledger(complete 384s 36p)/etl_pipeline(complete 849s 39p)/distributed_scheduler(**blocked@test 1375s** watchdog kill), 能力覆盖 17/17 | **真实 bug 根因链**: ①verify_result rc=None——运行时 FlowRegistry 持久 store(~/.regime/flows/code_workflow_v13.json)残留**旧 sg docker -c 包装**的 verify 命令, 真源 ops/flow_v13.json 是纯 docker exec; build_verify_argv 因 tokens[0]=sg ≠ docker 白名单拒绝(白名单拒绝是正确行为, 配置漂移导致) ②reviewer_inquiry 正确质询(23:24:57) ③dispatch_error transport timed out(developer 重跑验证时 POST 瞬时超时) ④monitor_abort(23:30:09, 300s 无 SSE 活性, 默认策略无 soft 直接 kill) | **根治**: ①新建 `core/verify_spec.py`——VERIFY_ALLOWED_EXECS+build_verify_argv 从 app 上移到 core(纯函数无 I/O, 单点真理), app/verify.py 复用保持对外 API ②`core/validate.py` `_check_capability_boundaries` 增加 **verify 白名单静态预检**——非 docker-exec 形态在 deep_validate 期即拒绝(注册/校验期失败而非运行中), 新增测试 test_verify_whitelist_shape_enforced ③store reload 修复(纯 docker exec) | 关键工程判断: ①"白名单拒绝是正确行为, 但配置漂移到非白名单形态只会在运行中才失败"是运行可信度缺口——校验期失败 >> 运行中失败; ②verify 命令形态校验是纯函数(无 I/O), 应住 core 层被 runtime runner 与静态 validator 共享, 避免两处复制(单点真理); ③默认 watchdog 策略(无 soft)对超长任务直接 kill 偏激进, dispatch 瞬时错误可被叠加放大为任务失败——超长任务建议配 soft 策略(先中断续跑再兜底) | 其余观测: shop_inventory host_pytest 0p/0f 为归档后立即跑 collection 瞬时竞态(复验 29p); 设计门真实质询(conf 0.9); 4/5 complete 零 ladder 零误杀 | general只读review: 0 blocker + 1 warning(W1: store 装载路径 _load_store 不经过 deep_validate, 残留 verify 仍会在运行中才失败) → **W1 已闭环**: `FlowRegistry._load_store` 装载时对每节点 verify 做形状校验(verify_command_error), 残留非白名单命令在装载期响亮拒绝+跳过(WARNING 记录); 新增 test_store_residual_verify_whitelist_rejected_at_load; 全量 612 passed(610+2) | next: commit | escalate: no
- [DONE] 2026-08-15(凌晨) **verify 修复验证：distributed_scheduler 重跑 complete（闭环实证）** | verified: 单任务重跑 outcome=**complete**(1504.9s, ~25min) + 宿主外部 pytest **26p/0f** + verify_result **rc=0**(test 门拿到真实 pytest 证据) + 3 verdicts | 对比首轮(修复前): blocked@test(1375s watchdog kill) + verify_result rc=None(白名单拒绝 sg 包装) + 4 verdicts + 2 设计门质询 | 结论: 同一超长任务在 verify 修复后由 blocked 转 complete——证明 verify 白名单配置漂移是首轮失败**直接根因**, 修复(白名单静态预检 deep_validate + _load_store 装载期校验 + store reload 清理)闭环; verify 在 test 门真正提供运行时证据, 语义门+确定性 gate 全链路恢复 | 归档: tasks_docs/nightly_run_archive/recheck-verify-20260815-002235/ | 报告: tasks_docs/quality_report.md §8.5 | 全量 612 passed 零回归 | next: commit | escalate: no
- [DONE] 2026-08-14(深夜后) **文档体系 + 自说明体系全方位同步（下一session主线交付）** | verified: 610 passed零回归 + sync_templates/check_capabilities/守卫(test_package/test_cli_doc_guard/test_config_doc_guard/test_capabilities_map)全绿 + general只读review两轮(首轮 1 blocker+3 warning+3 nit 全修, 复核 8/8 通过) + 真实冒烟(`regime regime design`→`--regime-name` 28.5s complete) + mkdocs build 本地 2-3s 可构建 | 交接审计确认 guide/howto/README 对五阶段新特性**零提及**, 本次全量补入 | **读者层(guide 8篇+howto 7篇+README/README.en)**: 制度一等公民(`regime regime design`/`--regime-name`)、整制度设计(flow+roles+watchdog+handover)、意图级 design(监督/人工确认→整制度; 审查前必须跑测试→judge 加 verify, 不强制切换整制度形态)、`~/.regime/hooks.py` 扩展点、ask_human+decide、verify 白名单 | **参考/架构/子系统复核**: 01_cli `regime regime` 组完整性确认(list/inspect/design/load/reload/rm 全入册); architecture/01 补阶段0监督统一修正(process-in watchdog 会话级, 进程外 T1/deadline/meta); architecture/02 已覆盖阶段2/3/4 确认无误导; 03/04 复核无改; subsystems/01_drive 修正监督归属(删旧"进程外T2盯anchor"表述)、06 意图级表述修正、07 补 regime 契约; 02/05/08/09 复核无误导 | **自说明体系**: 插件 `.opencode/plugins/regime-dialog-control.js` 补 `regime_regime_inspect/reload/rm` 三工具 + `regime_run` 转发 `--regime-name`(flow/regime_name 二选一、regime_name 优先, 与 CLI `_sm_from_flow_or_regime` 一致); 19→22 工具; dialog-control.md 补全整制度管理命令与 run 参数; description 补 file 键; 模板漂移守卫绿 | **智能体说明书(用户明确要求)**: 新建 `.opencode/agent-handbook.md`(agent 视角完整手册: 系统是什么/能力总览/命令面/22 工具表/制度与扩展点/ask_human 交互/典型流程/踩坑/真实起栈), sync_templates FILES 登记 → `data/agent-handbook.md` 随 wheel 分发(实测 wheel 含该文件), test_package 新增漂移守卫 | **mkdocs 本地挂起(运维)**: 交接记载的本地 build 挂起**当前不复现**(2.3s 完成, mermaid2 只注入 script 标签不下载, build 离线), 本地验证路径可用; 仅 1 已知 warning(README↔index, CI 已注明弃 strict) | review 价值: 抓出 1 blocker(guide/03 裸 `regime design` 不可执行——真实 CLI 是 `regime regime design`, 对话框是裸 `design`) + W3(意图级 design 触发条件与确定性 prompt 不符——"验证测试"非整制度触发, 四处 guide/howto + dialog-control.md + subsystems/06 统一修正) + N2(插件工具表混写)/N1(悬空 §4.1 引用)/N3(description 缺 file 键) | next: 顺延候选 V-2 PyPI(待用户 token) → P-005 覆盖率 → 限并发耐久 → GitHub Pages(待用户) | escalate: no
- [DONE] 2026-08-14(晚) **体系化重构阶段1a/1b/1d: Regime 一等公民（根因A落地）** | verified: 552 passed零回归(512+40) + general只读review两轮(2 blocker+4 warning+nits全处理) + 真实worker `--regime-name`冒烟14s complete端到端可用 | 落地根因A(运行制度碎片化在6个载体): 运行制度升为一等公民, 拥有与flow相同的完整生命周期(compile→deep_validate→preflight→hot-reload→version→permission→audit) | **新增 regime.py**: Regime聚合对象(flow+roles+watchdog+handover+stall/auto_resume) + compile_regime/validate_regime(统一编译校验门: watchdog负/零/bool阈值响亮拒绝、空handover拒绝、roles非dict/未知字段拒绝、name安全字符集防路径穿越、原子写) + RegimeRegistry(命名制度单一真源+持久store+原子热重载失败保留当前版本) | **构造收敛**: StatechartDriver.__init__接受可选regime(制度权威源, 阈值优先settings)+from_regime类方法; WorkflowUnit context_policy注入(制度handover优先settings JSON); Drive regime参数 | **CLI**: regime子命令组(list/inspect/design/load/reload/rm, 权限门禁design/load/reload/rm=RUN) + run/drive --regime-name(解析同一持久store); 死代码守卫接入regime.py(全部公共API有生产消费者) | **review两轮抓真实bug**: B1裸RegimeRegistry()不加载store→--regime-name永远解析不到(修: 用_default_regime_registry); B2 _run丢弃已解析sm→--flow跑错流程(修: _driver_from_options复用sm); W1 run --async漏转--regime-name; W2 load --name绕过安全字符集路径穿越(修: register/load统一_check_regime_name) | 关键工程判断: ①Regime=制度声明(per-task可变), Settings=运行环境(全局基础设施), 两者分离是制度一等公民的前提——不把watchdog_policy_json塞回settings而是进制度声明; ②"不做兼容修复"≠不保留底层直接构造(StatechartDriver(settings, sm, ...)是底层API, from_regime是制度入口, 二者归一内部); ③同名store解析bug(review抓出)证明"持久store单一真源"必须同一注册路径, 测试需覆盖正向路径非仅unknown; ④真实冒烟(design smoke→run --regime-name smoke 14s)证明整制度运行真实可用 | next: 阶段1c(独立supervisor判定统一到watchdog_policy, 行为语义重设计) 或 V-2 PyPI待用户 | escalate: no
- [DONE] 2026-08-14(下午) **体系化重构阶段0: 监督统一抽象收敛（W1/W2根治 + 可观测性修复）** | verified: 512 passed零回归(506+6) + general只读review(1 blocker+4 warning+4 nit全处理) + 真实worker drive冒烟124s COMPLETE supervisor=workflow_done无回归 | 用户授权破坏性重构(无历史包袱/无兼容修复/tricky), 从体系化宏观分析确认未完成任务(W1-W6+交接硬编码+自定义缺失)收敛到3个体系化根因(Regime非一等公民/监督职责分裂/核心语义未在底层定义), 蓝图`tasks_docs/_regime_redesign.md` | **根因实证(B)**: W1代码级定位=双看门狗阈值竞态(StatechartDriver用settings.stall_sec=120 vs Drive默认stall_sec=60, 外部T2先触发硬abort→workflow BLOCK→drive stop_when结束, in-process恢复阶梯永远到不了) + watchdog_fire只发内存bus不落journal(诊断盲区: "journal无watchdog_fire"≠未触发) | **修复**: ①drive模式supervise_sessions=False, 会话级监督归in-process watchdog(经REPORT跟随wait_sid, 会话旋转不失焦=W2结构性消除), 进程外只留T1/docker重启+全局deadline ②meta在drive模式退为"对journaled watchdog_fire的智能第二意见"(_meta_review_fires, meta_analyze参数化可复盘rotate后新会话; 智能建议不推翻已执行确定性动作) ③WatchdogUnit注入reporter, fire落共享journal ④SseActivity共享单一活性源(Drive注入, _owns_sse生命周期, try/finally防泄漏) ⑤CLI drive --stall默认None仅显式覆盖config(防覆盖用户配置), async argv补--meta/--meta-model(修既有缺陷) ⑥parallel/Drive去死参数stall_sec | 关键工程判断: "双看门狗职责收编"不是打补丁而是按Observer→Judge→Actor单一抽象重构; meta挂回监督链的方式是"journal作为进程内外唯一通信事实源"; 文档同步(01_cli/subsystems/04职责边界) | next: 阶段1 Regime一等公民(flow+watchdog+handover+role+verify+hooks合一) 或 V-2 PyPI待用户 | escalate: no



- [DONE] 2026-08-14(深夜) **体系化重构阶段4: 对话框意图级制度操作面（ask_human 人工确认点 + 意图级设计）——五阶段全部完成** | verified: 610 passed零回归(609+1) + general只读review(3 blocker B1/B2/B3全修 + W2-W6全修) + sync_templates/check_capabilities/守卫全绿 | **ask_human(根因A易用性兑现, W-自定义)**: core/models Action+ask_human + ReviewerVerdict.human_question; contract 一致性/置信度0.6/human_question必填; workflow _PH_HUMAN相(独立于节点deadline用human_confirm_timeout_sec)——_begin_human_checkpoint写黑板{wid}.human_ask/human_waiting/human_decision, _step_human轮询(决策单次消费+类型守卫, yes→advance/no→_route_human_rework带评论回开发者重做重审含detect_loop收敛, 超时human_default_on_timeout Literal block|advance|rework), human_wait上报status=idle防看门狗误杀, _clear_human_checkpoint消费清全部键; settings两字段; 对话框decide命令(裸decide只读列表不门禁, decide <wid> <yes|no> [评论]写门禁) | **意图级设计**: _run_design_nl提示词升级——NL需求→flow或整制度JSON(明确提到监督才加watchdog阈值保守, 审查前必须验证测试→judge带verify); compile_spec紧凑白名单补verify/readonly | **review 3 blocker**: B1 reviewer SYSTEM_PROMPT无ask_human→产测端永远发不出(功能生产不可达)——补prompt; B2 紧凑白名单丢verify(意图级设计的安全承诺静默丢失)——补白名单; B3 决策消费只清human_decision不清waiting/ask→decide契约被击穿——_clear_human_checkpoint清全部键 | 工程判断: ①"你只需对话"的闭环=意图级design(说需求→出制度) + ask_human(该人拍板时冻结等人)两个方向; ②人工确认点的无人值守安全=超时默认block(不静默放行) + 上报idle防监督误杀; ③紧凑/整制度两条流程编译路径必须字段一致(verify/readonly 不得被白名单静默剥离)——审查抓出的是"同一特性两形状行为不一致" | **体系化重构五阶段(0监督统一/1 Regime一等公民/2统一扩展点/3语义契约/4对话框意图级)全部完成**, 蓝图总结入本WORKLOG, _regime_redesign.md删除 | next: 常规推进(ask_human/意图级设计真实E2E冒烟) 或 V-2 PyPI待用户 | escalate: no
- [DONE] 2026-08-14(深夜) **体系化重构阶段3: 语义契约下放（W3 瞬时错误分类 + W4 reviewer 容错）** | verified: 602 passed零回归(599+3) + general只读review(0 blocker, W1标记过宽/W2 judge不对称/W3测试缺口全修) + 真实worker冒烟87.5s complete无回归 + sync_templates/check_capabilities绿 | **W3(瞬时错误分类)**: infra/opencode.py 新增 is_abort_error——Message.error 分类, 仅锚定 MessageAbortedError/generation aborted/aborted by user 为真 abort; HTTP/限流/网络=瞬时可恢复。窄匹配(review W1: 裸"abort"子串会把 ConnectionAbortedError"connection aborted by peer"误判为 abort, 收窄); workflow._latest_abort 只判真 abort BLOCK, 瞬时错误继续轮询受节点 default_deadline_sec 上限(默认600s非无限挂); _latest_transient_error/_audit_transient 节流记 message_transient_error 审计((sid,err[:300])键不刷屏); _latest_assistant 跳过 error 消息——judge 路径与 agent 对称(review W2: 瞬时错误文本不再被误解析为 reviewer 判定) | **W4(reviewer 契约容错)**: extract_json 增尾部逗号容错——_strip_trailing_commas 字符串安全地删 ,}/,] 前逗号(绝不破坏字符串内", }"), 只在原样解析失败后修复, 真坏截断仍拒绝 | 测试+9: is_abort_error 分类(含"连接含 abort 字样的网络瞬时反例")/_latest_abort 区分/瞬时不 BLOCK 继续轮询/审计事件节流/judge 瞬时错误不解析/持久瞬时错误节点 deadline TIMEOUT/尾部逗号+字符串安全 | 工程判断: ①瞬时错误正确语义=可恢复(继续轮询)≠死等, 由节点 deadline 兜底不是无限挂; ②abort 判定用"窄锚定+completed无finish形状"双信号, 避免网络瞬时误杀; ③reviewer 契约容错只做字符串安全的机械修复(尾部逗号), 不做"散文转判定"的语义猜测——后者会削弱确定性门 | next: 阶段4对话框意图级制度操作面(design意图级+ask_human确认点) 或 V-2 PyPI待用户 | escalate: no
- [DONE] 2026-08-14(深夜) **体系化重构阶段2: 统一扩展点模型（W5 RCE 根治 + W-硬编码 去硬编码 + W-自定义 对话框装配）** | verified: 593 passed零回归(583+10) + general只读review(2 blocker B1/B2全修 + W1/W2/W3/W6/W7修, W4/W5记录已知限制) + 真实worker hooks冒烟(REGIME_HOOKS临时插件, node_enter 6节点全fire, 90.3s complete端到端) + sync_templates/check_capabilities/守卫全绿 | **统一注册表(extensions.py)**: HookRegistry(register_hook/@reg.hook 6类生命周期hook + register_rule看门狗规则 + register_tool委托core.tools + fire审计式收集on_error不杀治理循环 + reload原子快照/工具清理) + load_user_hooks(默认~/.regime/hooks.py, env REGIME_HOOKS覆盖; 缺文件=空注册表, 导入失败/缺register=构造期响亮失败) | **hooks穿透全链**: StatechartDriver/StatechartCluster/WorkflowUnit/WatchdogUnit/Drive/Parallel/CLI(_load_hooks)/dialog_app; workflow埋5点(node_enter/node_done/transition/judge_verdict/handover), watchdog埋stall | **handover声明式化(W-硬编码)**: ContextHandoverPolicy增document_template/opening_template(.format风格); 优先级=handover hook覆盖(返回{"document":..,"opening":..})>声明式模板>内置构建器; _handover_package统一_rotate_session与_apply_transition | **verify白名单化(W5消RCE)**: build_verify_argv只允许`docker exec {container} <VERIFY_ALLOWED_EXECS>`形态(pytest/python/node/bash/sh...); argv执行shell=False绝无宿主shell解释, 爆炸半径限定worker容器; docker组过期自动回退`sg docker -c <shlex.join(校验后argv)>`(宿主shell只见再引号化的已校验argv, 元字符无法逃逸); 非白名单=响亮失败证据; ops/flow_v13.json verify改白名单形态 | **对话框hook装配(W-自定义)**: hook list/path/reload(reload写、权限门控) + help/capabilities | **review抓真接线bug(B1/B2)**: dialog_app先建cluster后赋hooks→watchdog永远收不到hooks(stall静默失效)——改构造时传入; StatechartCluster不并入hooks.rules→run-many/dialog静默丢用户看门狗规则——__init__统一with_rules合并 | 工程判断: ①hooks=策略层观察者, 只handover一个hook按契约返回覆盖——内核不变量I1/I2/I3不破; ②插件加载fail-fast vs hook运行异常审计, 二者分开才是正确语义; ③W5的RCE本质是宿主任意shell, 白名单化到docker-exec+argv把爆炸半径从宿主移到worker容器; ④统一注册表是"用户面单一入口", 内部按通道分流(tools委托既有core.tools), 不强行再造注册表 | next: 阶段3语义契约下放(W3 Message.error区分/W4 reviewer契约容错) 或 V-2 PyPI待用户 | escalate: no
- [DONE] 2026-08-14(晚) **体系化重构阶段1d: run-many/drive-many --regime-name + 对话框制度设计入口** | verified: 561 passed零回归(551+10) + general只读review(0 blocker, W1/W2/W3全修) + 真实worker `run-many --regime-name`冒烟16.5s complete端到端 + sync_templates/check_capabilities/守卫全绿 | **并行收尾(制度一等公民落地到并行面)**: ①`StatechartCluster.from_regime`(制度watchdog policy/阈值优先settings, roles+handover默认继承制度、显式覆盖仍生效——修review W2"roles静默回落default") + `add_workflow`增context_policy ②`Parallel`增regime参数, `_make_drive`把制度传入每成员Drive(drive-many --regime-name = 并行个drive --regime-name) ③CLI run-many/drive-many增--regime-name(async转发/preflight用制度flow/未知名fail) ④插件regime_run_many增regime_name | **对话框制度设计入口(1d)**: `design <name> <regime JSON>`(含flow键=整制度)注册进RegimeRegistry持久store(与CLI `regime regime design`同一真源); `_run_design_nl`识别regime-shaped回复; 新增regime list/inspect只读命令; `_design`的regime检测识别flows键+制度字段(修review W3: 全描述符+watchdog字段不再静默丢弃而响亮报错); 路由regime在inspect前(修W1死分支"regime inspect"被抢占); 插件新增regime_regime_design/regime_regime_list工具; dialog_app注入持久store | 测试: cluster from_regime(制度policy/roles/handover接线+真实跑通)、parallel制度转发(Drive收到regime)、CLI run-many/drive-many resolve+from_regime+未知名fail、dialog regime design/list/inspect/write-gate/W3回归 | 关键工程判断: ①"制度一等公民"的并行兑现=StatechartCluster.from_regime + Parallel.regime, 让run-many/drive-many与run/drive用同一运行规则, 不留"并行面只用flow"的残缺; ②roles/handover默认继承制度是"不静默丢失制度"的边界(显式覆盖仍允许)——审查W2抓的是"方法本身是陷阱", 修在类内而非仅调用方; ③对话框design成为整制度入口="你只需对话"向制度层推进的一步, 与CLI共享持久store使对话框设计的制度可被另一进程--regime-name运行 | next: 阶段2统一扩展点模型(~/.regime/hooks.py+统一注册表+handover声明式模板+verify白名单化) 或 V-2 PyPI待用户 | escalate: no
- [DONE] 2026-08-14(晚) **体系化重构阶段1c: 独立supervisor判定统一到watchdog_policy规则引擎** | verified: 551 passed零回归(550+1) + general只读review(0 blocker, 修review抓出的共享引擎不变量W1) + 全量551 passed | **根因B"第二套判定实现"根治**: 删除`SessionWatch`/`_verdict_for_stall`(独立supervisor T2原自研判定), 统一到watchdog_policy规则引擎 | **引擎参数化**: `Ladder.order` + `WatchdogPolicy.actions`(动作词汇按Actor能力位声明: 进程内nudge/interrupt/resume/fallback/kill, 进程外abort/fallback_model/restart/human; 规则校验/阶梯/decide全走self.actions) | **supervisor重构**: `external_policy(stall_sec)`绝对静默时长多级规则(stall_sec→abort/2×→换模型/3×→重启/4×→人工, 等效旧连续窗口逐级升级但判定源单一) + `_evidence`构造SessionEvidence+恢复旗标(SSE新鲜活性/idle重置阶梯) + run循环T2=policy.decide(ev, recovered) | **meta语义定案(只升不降)**: `_meta_second_opinion`——meta可把确定性动作直接升到human, 绝不减少(确定性策略是安全下限, 智能不越确定性门, 符合阶段0定案) | **共享引擎不变量修复(review W1)**: decide合并meta_hits+hits按最严重裁决, meta-gated软规则不再屏蔽更硬确定性规则(meta是gate非short-circuit), 新增回归测试 | 工程判断: ①判定统一(Judge共享)vs动作词汇统一是两回事——1c统一前者, 动作按能力位声明, 强行硬塞统一词汇会造语义牵强映射(restart vs kill vs human不对齐); ②"绝对时长多级规则"重设计: 冻结4×stall_sec直接升human是正确语义(不用浪费中间abort), 恢复重置保每episode从abort开始; ③meta只升不降防"模型误判否决确定性动作"——正是阶段0"智能建议不越确定性门"的落地 | next: 阶段1d(并行+对话框制度入口) 或 V-2 PyPI待用户 | escalate: no



## 阻塞

（无）

## 自省记录

- [REFLECT] 2026-08-14(7) | progress: 体系化重构五阶段(0-4)全部完成——监督统一(989dac6)/Regime一等公民(bb73524,4e1f2f7,ea50be8)/统一扩展点(d1fe9f4)/语义契约(7a38e9a)/对话框意图级(3b06490)。阶段2-4本session推进, 610 passed零回归, 每阶段general只读review 0 blocker收口, 真实worker冒烟全complete(hooks 6节点/run-many 16s/语义 87.5s/阶段4 88s), W类遗留全部关闭, 工作簿删除, HANDOVER交接完成 | 关键工程判断: ①五阶段的顺序(先定判据再定制度再扩展点再语义再易用性)让每层有地基: 阶段1c判定统一是阶段2规则注入的前提, 阶段2扩展点是阶段4 ask_human的载体; ②"统一动作语言+能力位"vs"按能力位参数化词汇"——1c选后者(judge共享、Actor词汇声明), 避免强行映射restart/kill/human的语义牵强; ③每阶段review都抓出"测试绿但生产不通"的真实缺陷(B1 dialog watchdog半接线、B2 cluster不并规则、B1 reviewer prompt无ask_human、B2紧凑白名单丢verify)——证明general只读review在测试之外的真价值; ④临时文档纪律执行到位(工作簿完成即删, 总结入WORKLOG) | risk: 阶段4的ask_human/意图级设计只有单测+正常路径真实冒烟, 未做"真实worker触发ask_human→decide"的完整E2E(需构造reviewer返回ask_human的流程, 留下一session); 紧凑/整制度两路径字段一致已修但真实LLM生成的整制度质量未实测; dialog-control容器仍是旧wheel(仅A路验证窗) | next: 下一session真实E2E冒烟ask_human+意图级design, 或V-2 PyPI待用户 | escalate: no

- [REFLECT] 2026-08-14(6) | progress: 体系化重构阶段1a/1b/1d完成(commit bb73524)——Regime一等公民, 552 passed零回归 | 关键工程判断: ①"制度(Regime)=per-task可变声明, 环境(Settings)=全局基础设施"的分离是制度一等公民的前提——watchdog/handover策略从settings JSON迁入制度声明是方向而非兼容修补; ②新增基础设施模块(regime.py)必须立即接入生产消费者并纳入死代码守卫, 否则"半接线"债复发——接线方式=CLI --regime-name(真实可用)而非只造对象; ③两轮review抓出的B1(裸registry不加载store)/B2(_run丢sm)证明: 重构中最易错的是"同名store多条解析路径"与"参数沿调用链被丢弃", 测试必须覆盖正向解析路径而非仅unknown分支; ④"不做兼容修复"≠不保留底层直接构造——StatechartDriver(settings,sm,...)是底层API, from_regime是制度入口, 归一内部而非留两套行为; ⑤RegimeRegistry复用FlowRegistry模式(持久store+原子热重载+失败保留)证明"统一生命周期"可复用既成架构而非另起炉灶; ⑥真实冒烟(design→run --regime-name 14s)是"命令可用"与"对象存在"的分界 | risk: 阶段1c(独立supervisor判定统一)是行为语义重设计(连续窗口vs绝对时长多级规则), 需慎重避免破坏独立supervisor的T2阶梯语义; fallback阶梯未接线(_on_escalate仅记录); 对话框制度设计入口未做(1d补全) | next: 阶段1c 或 V-2 PyPI待用户 | escalate: no
- [REFLECT] 2026-08-14(5) | progress: 体系化重构阶段0完成(commit 989dac6)——监督统一抽象收敛, 512 passed零回归 | 关键工程判断: ①"维修一两个bug"vs"体系化重构"的分界=先做宏观根因分析(所有已知问题收敛到3根因)再动手, 而非逐 bug 打补丁——W1/W2/W5/硬编码其实共享"制度碎片化+监督分裂"一个根; ②监督职责收编的体系化落点=Observer(唯一活性源)/Judge(唯一判定引擎)/Actor(按能力位)三层, drive模式会话级监督只归in-process(进程内可跟随wait_sid+有完整恢复阶梯), 进程外只留独有能力(T1/docker/deadline/meta); ③meta挂回监督链的正确形态="journal作为进程内外唯一事实通信源"——进程外supervisor轮询journaled watchdog_fire做智能第二意见(建议不越确定性门), 而非进程外再做一套T2判定(那正是双看门狗竞态之源); ④watchdog_fire落journal是"可观测性即正确性"的实例——诊断盲区本身扭曲了根因定位; ⑤reviewer(general)抓出drive --meta死参数与SseActivity异常路径泄漏两类review价值; ⑥真实worker冒烟(124s complete)证明重构无回归 | risk: 阶段1(Regime一等公民)是大范围构造签名/配置/CLI破坏性改动, 需谨慎分步; fallback阶梯仍未接线(workflow._on_escalate仅记录); 独立supervisor判定仍是自研(SessionWatch)待阶段1统一到watchdog_policy | next: 阶段1 Regime收敛 或 V-2 PyPI待用户 | escalate: no


- [REFLECT] 2026-08-14(3) | progress: WORK_PLAN13 复查超长任务(distributed_scheduler 分布式任务调度器, 1127.5s, 1510行/13文件, 宿主pytest 26/0)——四项机制全部真实触发: 设计门两次issue_pending质询(只读understand让设计门审未实现方案, 强制四项设计决策书面定稿)、test门verify宿主pytest证据(rc=0, docker exec)、wrap节点真实上下文交接(usage 84%, 新会话凭交接文档完成wrap→complete)。复查暴露并修复三个真实bug: ①drive外部supervisor T2 abort会话后workflow永久轮询死会话死锁(外部abort哨兵→诚实BLOCK) ②B1 pause→resume窗口自家abort哨兵被误判死会话(review发现的真实冲突, 加_own_abort标记区分) ③reviewer复杂判定反复输出散文未过纯JSON门(extract_json重写为平衡括号鲁棒解析+SYSTEM_PROMPT强化+重试2→3) | verified: 506 passed零回归 + general review(2nd) B1/W1/W2全修 + preflight complete + 归档tasks_docs/nightly_run_archive/20260814-wp13-recheck | 关键工程判断: ①only下readonly understand + 设计门=设计门从"审已实现代码"变"审方案", 质询频率显著上升(pay之前的0-1次→本任务2次)——这正是设计模板的本意; ②context_handover在wrap节点84%触发并成功交接证明机制可用, 交接文档+新会话开场真实有效; ③真实长跑暴露的bug(死锁/JSON合规)单测永远测不到(mock的send_message不产生真abort哨兵、mock的reviewer默认输出干净JSON), 只有真实worker+真实模型能逼出来——复查任务价值巨大; ④drive的外部supervisor T2与in-process watchdog职责重叠且不协调是架构债(外部abort先于in-process watchdog触发), 已用workflow侧死锁保护兜底, 彻底收编留待WORK_PLAN14 | risk: in-process watchdog默认策略在真实drive下未触发(stall时外部supervisor先动手), 恢复路径(pause/resume/fallback)在drive模式仍未实证; judge空reply哨兵已修复但recovery归因(瞬时故障误报stall)语义略宽 | next: WORK_PLAN14(meta/chaos接入复查) 或 V-2 PyPI待用户 | escalate: no

- [REFLECT] 2026-08-14(2) | progress: WORK_PLAN13 核心实现——语义门(ReviewerVerdict.issues[blocking|warning], gate拒绝advance+blocking, 兼容旧输出) + 节点能力边界(Node.readonly, 模板understand/read_code只读, 强制先设计后实现) + 运行时验证(Node.verify宿主命令, docker pytest证据进judge prompt, 失败注入blocking确定性拒绝, verify_enabled默认false opt-in) + 上下文预算交接(context_handover_policy_json, soft询问/硬强制, 真实交接文档+新会话开场, 修复stale reviewer缓存) + [WORK_DONE]标记对齐 | verified: 497 passed零回归(469+28) + general只读review(3 blocker B1 stale reviewer/B2节点错位/B3 verify不阻断 + 7 warning全修) + 真实冒烟(简单任务209s complete, 设计门对简单任务也两次质询scope-drift+设计不定=readonly生效的直接证据) | 关键工程判断: ①"只读understand"一个改动就让设计门从形式变实质(质询从无到有)——节点能力边界是流程质量的最大杠杆; ②verify失败确定性阻断的正确语义: 审查者仍保留ask_developer/request_context通道, 阻断的不是"提交问题"而是"掩盖问题"; ③交接文档由驱动确定性构建(消息+节点+任务+汇报)而非依赖模型自写——可靠; ④B2教训: 上下文检查只查"下一节点角色会话", 逐会话rotate会把错误节点上下文写进别人的交接文档 | risk: 只读understand改变任务动力学(全部编码移到implement, 单节点压力更大); 需要真实长任务复查验证 | next: 复查超长任务(本记录之后) | escalate: no

- [REFLECT] 2026-08-14 | progress: 夜间整合重跑(WORK_PLAN8阶段5+WORK_PLAN9验证)——直接全量开跑`bash ops/run_nightly.sh --root /tmp/nightly-run-20260814`(后台setsid+轮询监控), 4/4复杂任务complete(shop_inventory 349s/kv_cluster 664s/payment_ledger 499s/etl_pipeline 515s), 宿主独立pytest全0 failed(63/22/27/28 passed, 140断言), reviewer verdicts 2/2/3/2(实质判定), 能力覆盖17/17(0 uncovered), 全量归档入库(tasks_docs/nightly_run_archive/20260814-012700, 会话快照+工作区+切片+report), 全量469 passed零回归 + sync_templates/check_capabilities守卫绿, 文档同步(MAIN_TASKS/quality_report/WORKLOG/HANDOVER) | 关键工程判断: ①运行根必须用全新目录(/tmp/nightly-run有上次残留, 复用会混数据) ②bash工具对长跑进程用setsid+nohup持久化, 轮询quality-report.json(每任务即写)而非stdout(tee缓冲看不到实时) ③本次零ladder/零误杀证明WORK_PLAN10(SSE活性)与WORK_PLAN11(策略引擎)修复在真实复杂任务下有效, 对比旧lru_ttl首轮7次截断→human已系统性消除 ④4任务全部首轮一次完成, 无诚实失败路径触发(gate/human), 说明复杂任务套件在DeepSeek官方API下稳定 | risk: 单轮4任务验证无失败路径样本(诚实失败已由旧43次运行覆盖); 归档~2.3M入库 | next: V-2 PyPI待用户 或 P-005覆盖率优化 或 限并发耐久二次验证 | escalate: no
- [REFLECT] 2026-08-13(4) | progress: 复核+改进计划执行——用户指示仔细复核, 复核确认前轮A-F六项结论(F更正: 低置信度advance非缺陷, gate已有advance≥0.5阈值), 实施4项代码修复+1文档+1 UX(A: supervisor SSE异常/无类型审计+_safe_record防日志失败杀loop; B: workflow识别abort中断消息(error/finish is None)不推进, 其余finish推进交reviewer; C: preflight诚实提示_note修复NameError; D: report_len_warn审计; E: 文档失实; 配套MockClient finish="stop"对齐真实契约) | verified: 415 passed零回归(407+8) + 真实regime run 56s COMPLETE + general review(2nd) 1 blocker+5 warning全修 | 关键工程判断: 复核的价值在于推翻自己的前轮结论(F)——先入为主的'问题'经代码核对后可能并非缺陷; B修复的最大风险是误伤正常完成(真实opencode finish='stop' vs 中断None, 实测区分可靠), 但'其他finish值都推进'是关键权衡——宁可reviewer拒收rework也不死等超时(死等白烧deadline); 测试假体默认值与真实dataclass不一致会掩盖契约问题(测试finish='stop' vs 真实None), 修MockClient显式对齐比改判定逻辑更根本; 真实worker验证不可省(仅单测无法证明'正常完成仍推进') | risk: 本轮src改动需重建容器镜像才在真实长跑生效; _events_no_type累积计数语义=累计未解析数可接受; MaxListeners仍为opencode内部问题 | next: 夜间重跑质量套件验证lru_ttl首轮不再误杀(可选) 或 V-2 PyPI待用户 | escalate: no

- [REFLECT] 2026-08-13(3) | progress: 深度核查执行——从43次运行选择性深挖4个失败任务+3个有ladder成功对照组, 全事件链还原+journal全文+对照内核代码, 发现并修复真实系统bug: opencode 1.18.11 /event SSE无event:行→event_stream raw["event"]恒None→T2活性兜底永久失效(长生成被误判stalled→abort打断输出→截断草稿)+journal被90%delta噪音淹没; 修复data.type回退, 409 passed零回归, general review无blocker | verified: 409 passed(407+2) + 真实worker SSE生效 + 报告tasks_docs/quality_deep_check.md | 关键工程判断: 用户'深度核查'指示价值巨大——此类bug pytest永远测不出(测试mock用event:行格式, 真实worker无event:行, 测试绿但生产行为不同='测了个寂寞'的底层形态), 只有对真实运行痕迹(journal/events)做逆向对比才能暴露; 事件链+journal双源交叉验证法有效(events给时序/verdict, journal给报告长度/ladder/liveness证据); '首轮自愈'实为误判偶然性而非系统能力, 修复T2活性信号后系统性消除, 比'相信自愈'更可靠 | risk: 修复仅src层, 运行容器需重建镜像才生效(下轮夜间套件重跑验证可选); quality_report.md的json_config描述与事实不符待更正; MaxListenersExceeded为opencode内部问题已确认非本仓缺陷 | next: 夜间重跑质量套件验证lru_ttl首轮不再误杀(可选) 或 V-2 PyPI待用户 | escalate: no

- [REFLECT] 2026-08-13(2) | progress: 交接准备——清理全部运行进程(0残留), 质量套件与耐久数据(artifacts/events/journal/tasks/report, ~12M)全量归档入库(tasks_docs/quality_run_archive + durability_run_archive), 书写交接文档(下一session主线=内部代码质量+工作日志深度核查, 含数据地图/核查清单A-E/6条待深挖线索) | 关键工程判断: 用户'代码质量分析不能只靠pytest'的洞察正确——pytest全绿≠质量好, 深查要落在三处: ①产物代码本身(测试真实性/边界/安全/规格符合度) ②reviewer判定实质(是否过场/低置信度放行) ③工作日志流程(节点推进/developer跳流程/异常事件); 归档而非删除运行痕迹是'可复核'前提, 否则深度核查无据可查; 交接文档要点式列出待深挖线索(4个非complete根因 + worker监听器泄漏 + 跳流程), 下一session无需重新发现即可开工 | risk: 归档~12M入库使仓库增大(可接受, 核查证据); /tmp原数据保留双份(归档后/tmp可清, 但保留更稳) | next: 下一session执行深度核查 | escalate: no

- [REFLECT] 2026-08-13 | progress: 夜间质量收益验证完成——12个复杂工程任务套件(边界/异常/并发/强制pytest)经regime drive监督栈跑2h/43次, 最后一轮12/12 complete, 宿主外部pytest全0失败(239断言), reviewer每任务2-4次实质判定; 意外发现并修复真实bug: reviewer bash ask headless死锁(复杂任务逼reviewer跑pytest→权限ask挂600s), 改deny+只读白名单后graph_algos 85s complete | verified: 407零回归 + 双源一致性守卫 + sync漂移守卫绿 | 关键工程判断: 质量验证必须'宿主独立外部复验'(docker cp产物+宿主pytest), 否则worker内自跑测试是'自己验自己'不可信; 复杂任务(逼reviewer动手验证)比简单任务更能暴露系统边界——正是用户要的'确认工作能力'; reviewer死锁与god死锁同源(headless+ask), 原则是headless agent一律禁ask; 诚实失败模式(阶梯human/gate exhausted/blocked)全部正确终止且自愈, 证明监督体系不是摆设; 质量套件成本~100-230s/任务(真实模型), 适合夜间/手动回归 | risk: 4个非complete发生在首轮冷启动(developer截断循环/ reviewer gate耗尽), 具体根因未深究(模型行为/会话状态), 但系统均诚实兜底; harness未含资源采样(质量聚焦, 稳定性由durability报告覆盖) | next: 夜间稳态耐久二次验证(简单任务限并发基线) 或 V-2 PyPI待用户 | escalate: no

- [REFLECT] 2026-08-12(5) | progress: 术语整体改名(花哨术语清零)全量落地——控制对话框(dialog control)/并行任务(parallel)/安全看门狗(watchdog)/承载·接入, 941+81+多轮定点共~1100处替换, 覆盖内核代码/CLI/docker/插件/agent/提示词/全站文档/README/HANDOVER/测试, 30个文件重命名 | verified: 407零回归 + mkdocs干净 + 0死链 + mermaid 7/7 + 全树旧词残留grep=0(排除tasks_docs历史) | 关键工程判断: 命名是主观决策且影响公开仓库/CLI契约/Docker容器, 先评估影响面(450+处god)再请用户拍板命名而非自决; 用'最长优先精确子串替换'脚本而非正则, 规避god子串误伤(godaddy/goodyear/godzina在node_modules), 且node_modules/tasks_docs/site全程排除; 批量替换会破坏局部变量一致性(如test_parallel的batch/fleet混用致NameError), 需逐文件核对重写; docker镜像/容器/数据快照(data/docker)三处同名需同步改名+sync_templates重同步+漂移守卫; L0/L1/L2历史层代号与监督阶梯L1-L5真实概念需区分(只删前者); 中文替换无词边界, 靠全角引号与精确短语规避误伤 | risk: 公开CLI契约变更(scaffold --assistants/容器名)属破坏性, 已在KNOWING_LIMITS/发布文档体现; 历史tasks_docs保留旧词(用户决定); 文档站GitHub缓存可能短暂显示旧词(无害) | next: 夜间稳态耐久二次验证(限并发) 或 V-2 PyPI待用户 | escalate: no

- [REFLECT] 2026-08-12(4) | progress: 文档图示方案评估(Mermaid主方案) + 7张图落地——mkdocs.yml/mermaid2(锁10.4.0) + CI docs.yml加依赖 + WRITING_GUIDE C.8图示纪律 + index全景图/00任务时序/00审查闭环/03设计成品/architecture-02信号时序/04-supervisor监督循环/06-fleet隔离图, 每图配图例说明 | verified: 407零回归 + mkdocs build 7图6页干净 + mermaid@10.4.0解析器jsdom环境7/7语法通过 + general review无blocker | 关键工程判断: 决定性约束是双端渲染(GitHub原生+站点), Mermaid是唯一'文本可diff+GitHub原生+MkDocs插件化'方案, 静态图/PlantUML/Graphviz/HTML内嵌/JS交互全部因GitHub端不可渲染或必然漂移否决; 图先做读者价值排序(任务时序>全景>审查闭环>阶梯), 简单链保留ASCII不升级(克制, 全站~10张); mermaid语法无法在MkDocs构建时验证(插件只做HTML包裹), 故引入node+mermaid@同版本+jsdom做解析器校验(7/7过)作为手工质量门, 记入纪律; 中文标签用「」全角引号规避mermaid语法引号冲突 | risk: 站点渲染依赖unpkg CDN(浏览器端), 已文档化自托管选项; GitHub与站点mermaid引擎版本不同, 极端图观感可能略异(用标准语法规避) | next: 夜间稳态耐久二次验证(限并发) 或 V-2 PyPI待用户 | escalate: no

- [REFLECT] 2026-08-12(3) | progress: 以'对项目一无所知的普通读者'视角通读全部对外文档并产出一份批评报告(tasks_docs/docs_reader_critique.md), 随后逐条深入代码核实真相并全面落地: guide编号去重/导航归位/去术语/去重/去stale/补图解 | verified: 407测试零回归 + mkdocs build干净 + 全仓相对链接0死链 + general只读review无blocker(修7 warning) | 关键工程判断: 文档问题的根因是'概念归属不唯一'(code_workflow表/worker-god表/CLI契约三处重复)与'设计定案文档与实现漂移'(architecture/02引用已删的Reviewer.judge/SegmentRunner, subsystems/07说契约未就绪, subsystems/09还写待实施)——前者按WRITING_GUIDE A.4收敛归属, 后者直接以代码真相改写而非贴免责; 中文标题锚点在MkDocs被slugify剥离(生成空锚), 故站内链接一律用无锚点文件链接(跨GitHub/MkDocs双端稳定); wheel用户'起栈'路径缺口(ops/up.sh不在包内)是真实功能缺口, 文档只能诚实标注替代路径并记候选 | risk: 05_god_dialog_contract只做瘦身未彻底删全量命令表(01_cli仍权威, 可接受); architecture/02仍是'设计定案+现状'混合体, 彻底纯现状化是大改写, 留作远期; 图解用ASCII(零依赖双端一致), Mermaid留作远期候选 | next: 夜间稳态耐久二次验证(限并发) 或 V-2 PyPI待用户 | escalate: no

- [REFLECT] 2026-08-12(2) | progress: 长跑暂缓后日间推进三项——L2收尾(drive自动journal保留) + 版本耦合护栏(doctor版本检查) + 示例flow/外部读者上手文档 | verified: 400 collected全绿(覆盖72%), 真实worker doctor全绿(版本兼容), 示例flow三路径全过 | 关键工程判断: drive保留策略放Drive.run收尾(非CLI)使可单测; 版本护栏用major.minor匹配(fail-open未知版本, patch漂移安全)且放doctor(advisory非blocking run); 示例flow的rework回环用max_total_nodes兜底(validate警告, 非无限循环); 心智模型文档定位为外部读者先读(解决'一人深研外人难上手') | risk: 版本护栏未知版本fail-open与KNOWN_LIMITS'匹配才通过'表述略宽松(已如实); 示例rework回环在真实运行若报告恒空会BLOCKED(max_total_nodes兜底); 剩余夜间稳态耐久二次验证+Pages待授权 | next: 夜间稳态耐久(限并发, 验证~100%完成率) | escalate: no
- [REFLECT] 2026-08-12 | progress: 长期耐久验证(WORK_PLAN6 I L1+L3)完成——2h真实运行零崩溃/停滞/重启, 资源线性有界增长, worker全程健康; "2h+能持续运行"声明成立 | e2e-real按用户决定封存(CI移除job) | 关键发现: 11/38 timeout全部命中600s deadline且根因是验证配置过度订阅(单worker每150s发一个积压至busy13)非系统缺陷——supervisor正确执行deadline产出干净error/timeout; session累积16→96(+80)是有界线性运营成本(记录不可删只能abort), 内存+231MB/2h约1.9MB/min, journal 3.4MB线性 | 工程判断: 耐久结论必须以event ledger为权威(event ledger干净, tasks_dir含跨run孤儿可误报); harness的tasks outcome必须读summary非注册表(注册表恒running); 验证过度订阅本身是真实运营教训(并发上限比单任务超时更敏感) | risk: L2资源治理未接入(长跑自动prune/清理需下轮); 本次未触发T2 stall→恢复路径(stall恢复能力靠既有E2E覆盖); session无上限累积是长期运营边界 | next: L2资源治理 或 V-1 Pages(待授权) 或 新一轮稳态耐久(限并发验证~100%完成率) | escalate: no
- [REFLECT] 2026-08-11(4) | progress: WORK_PLAN7 对外供给就绪 I-IV 全落地——模板进包(纯wheel用户 preflight 从"必败"变"ok:true") + scaffold 一键配置 + 单一真源收敛(根副本删除+CI漂移守卫+同步脚本) + 文档修复(死链清零/部署章节/发布教程06_release/KNOWN_LIMITS对外摘要) | verified: 389 passed(383+6, 覆盖71%) + 隔离wheel验证 | 关键工程判断: 打包派生用"提交副本+漂移守卫测试+同步脚本"而非构建钩子(简单/CI可见/可回滚), data/为wheel分发快照、真源=docker/*-config+workflow-regime/skills; scaffold去重reviewer.md(双源一致性测试兜底); 权限scaffold归RUN(写配置但幂等) | risk: data/副本与真源靠测试守卫(改真源忘sync会CI红, 属防呆非静默); V-1 Pages/V-2 PyPI 需授权/凭据未做(路径已文档化); 新增wheel构建回归测试~3s(suite ~75s) | next: V-1/V-2 待授权 或 转耐久验证(I WORK_PLAN6) 或 README精校 | escalate: no
- [REFLECT] 2026-08-11(3) | progress: 上帝对话框制度设计闭环(P0)完成——flow design inline注册 + status --deep 聚合态势 + run/drive --flow + 终止judge节点gate修复(发现"最终审查judge流程永远无法完成"的潜在bug) + god插件 regime_flow_design/summary/load/report; 真实E2E验证 god 设计→注册→执行闭环(被宪法重复检测拦截属运行期判定非机制故障); 官方模型驱动 drive --flow 31.4s COMPLETE 零 ladder | 用户授权默认模型切换 DeepSeek 官方 API(实测1.6s vs opencode-go 40s) | risk: opencode session 状态不一致(message 404+status busy, 容器重启后)会使 workflow 卡在死会话(已记 KNOWN_LIMITS, 规避=clean/重建); god A-route 长使命仍可能因 opencode 容器状态退化需重建; repetition 阈值 0.40 为经验值待更多样本 | next: 耐久验证(I) / README双语精校(V) / C3延迟参数校准 | escalate: no
- [REFLECT] 2026-08-11(2) | progress: 全自主清理+重构实践暴露的问题——①supervisor T2 stall 误报根治(SessionWatch 死参数 stall_sec 从未生效→一停顿即 abort; 重写为时间窗语义 + SSE 活动兑底(排除 server.connected 握手) + 恢复重置 consecutive + 每窗一射) + 真实 E2E 验证: 同任务 drive 从"连续5次误判 escalate→human"变为"198s COMPLETE 零 ladder" + ②僵尸进程 bug 根治(新增 infra/proc.py pid_alive 读 /proc/<pid>/stat 状态, Z/X 判死; jobs.py+task.py 接入 + 回归测试) + ③god 容器环境漂移(镜像打 git-head label, up.sh 自动重建; god 挂载当前 docs/插件/god.md; Dockerfile.god COPY docs) + ④A-route 权限死锁(god.md bash 白名单扩只读命令; god-window.md 文档化 /permission 应答) + ⑤reporter 噪音(drop message.part.delta) + task --tasks-dir + drive --ledger | verified: 347 passed(344+3, 覆盖71%) + 真实 E2E(198s COMPLETE, 0 ladder, 产物9/9测试绿) + god A路验证(读手册+工具调用无死锁) | risk: 镜像重建后 god 容器 version 串仍 0.2.0(未 bump, flow 命令已在); worker 镜像每次 HEAD 变自动重建 | next: 提交本批修复 → I 长期耐久验证(P0) → e2e-real激活(待key) → V README双语精校 | escalate: no
- [REFLECT] 2026-08-11 | progress: 发布就绪收尾——WORK_PLAN6 II 真实CI转绿(GitHub Actions py3.11/3.12+E2E success, 修secrets-job-if与worker ensure死api_key两真缺陷) + III 去硬编码(REGIME_BIN) + IV 技术文档体系彻底重构(参考docs-ref组织思路→Divio结构 docs/{guide,howto,reference,architecture,subsystems}+根索引, 删废弃历史文档, 全仓跨引用+独立general agent交叉核验全绿) + V 发布准备(SECURITY/CONTRIBUTING/README.en/MIT) + 项目公开上传GitHub(haberzero/regime-driver, push已授权) | risk: I 长期耐久(2h+)未做(需长跑/密钥); e2e-real 因无GitHub key secret 门控跳过; 文档重构量大但断链/红线/测试(333)全绿 | next: I 长期耐久验证(P0) → e2e-real激活(待key) → V README双语精校 → C3延迟调优(依赖I) | escalate: no
- [REFLECT] 2026-08-10 | progress: WORK_PLAN5 F1-F11 全落地——FlowRegistry(compile_spec/深检门/原子替换旧快照/持久store单一真源) + `regime flow` CLI(list/validate --watch/load/reload/rm/inspect, 权限门禁) + god A/B路接入 + 反循环; 归并 god self.flows 第二真源; 329测试全绿 + 真实CLI跨进程持久验证 | risk: FlowRegistry 单一真源为每进程作用域(CLI跨进程靠 disk store 统一; god/drive 各持实例), 运行中 workflow 依赖"不原地mutate"约定(已由不变量保证); F6 后半(运行中自动 reload-on-change)未做 | next: L1-L3 长期运行耐久性(opencode-go 2h+) / C1-C4 微调 | escalate: no
- [REFLECT] 2026-08-09 | progress: 按用户三个方向收口交接——①长期运行授权用 opencode-go ②主线转向"流程热编译/热加载基础设施"(WRITE WORK_PLAN5: FlowRegistry/热校验/CLI+dialog/反循环/长期运行/微调) ③其余按工程判断微调; 同步 HANDOVER §8 主线+优先级表、TASK 候选、测试基线300 | risk: 真实模型(opencode-go) judge 慢/停滞使舰队成员可能超时(系统以 deadline/supervisor 兜底); worker 须 root→工作区 root 文件(down chown); 覆盖率数值未测(pytest-cov 未装, C1 待办) | next: 交接文档已更新, 分支干净300全绿, 待下 session 按 WORK_PLAN5 从 F1-F4(FlowRegistry+热校验) 开工 | escalate: no
- [DONE] worker工作区隔离——转"多opencode实例每工作区一个"(用户指令) | verified: 285 passed(275+10) + 真实E2E隔离验证 | 新增 src/regime_driver/worker.py(WorkerPool: slugify/instance_name/work_dir_for纯函数 + get/ensure/list/remove docker持久映射 + no-duplicate不变量 + _run_docker sg回退(shlex.quote修多词format) + _resolve_key env/key文件回退) + `regime worker` CLI(list/up/base/down) + `regime drive --workspace <ws>`(解析工作区实例base_url) + tests/test_worker.py(10) + docs/DESIGN-worker-isolation.md + HANDOVER/config同步 | 真实E2E: up ws-algo→复用不重复(同端口)→up ws-infra独立→drive --workspace ws-algo 真实任务COMPLETE(119.6s)→产物ws_nine.py仅在该工作区(ws-infra与默认worker均无)=物理隔离成立 | 修: test_ensure_requires_key因新增key文件回退读到真key→health-wait 120s慢, 改mock _resolve_key="" (测试从78s回归)
- [REVIEW] worker多实例隔离 | 1 warning(测试慢,已修) | blockers: 0 | warnings: 0 | 核心不变量(每工作区一实例, 无重复)经docker持久保证; _run_docker用shlex.quote修sg多词format(此前get/list查不到容器); 隔离真实性经真实E2E验证(产物仅落该工作区); 权限: worker up/down为docker写操作, drive --workspace经drive门禁; 285全绿78s | 说明: 每实例一容器资源线性增; down会删容器(挂载目录宿主机保留)
- [DONE] P1#3 god容器镜像固化+一键起栈 | verified: ops/up.sh worker/god/all 实跑(重启+等健康) + god A路健康/回话真实验证 | 新增 ops/up.sh(一键构建+拉起+等健康, 自动sg docker fallback, key从env或~/.regime/keys/deepseek.key, --rebuild强制重建固化镜像) | 镜像已固化(Dockerfile.god COPY god.md+regime-god.js+god配置, 不再需docker cp+restart) | docs/howto/god-window.md 更新一键用法 | 说明: god A路工具调用turn慢(真实LLM+工具), 探测需>90s timeout, 非镜像缺陷
- [DONE] P1#4 CI接入真实E2E | verified: .github/workflows/ci.yml YAML OK + 本地模拟CI(REGIME_E2E=1全量E2E 4过1skip, 与CI行为一致) | ci.yml 增 e2e-real job(needs test, DEEPSEEK_API_KEY secret门控): 构建worker镜像→起容器→等健康→REGIME_E2E=1跑test_e2e_worker(restart测试因REGIME_E2E_RESTART未设自动skip)→cleanup; 让真实执行链成回归门槛防静默漂移 | 注: 本仓库禁push, CI为后续push后生效(本地已验证等价)
- [DONE] P2#5 B路演进(T8: 会话深交互+回收) + 测试金字塔/死代码守卫扩展 + **权限门禁补洞** | verified: 275 passed(266+9, 5 skip) | (1) GodDialogUnit 增 sessions[busy]/abort <sid>|--all/reclaim <sid>|--all(中止+删除回收) 写门禁, tests/test_god_dialog.py(+8), howto/god-dialog更新 | (2) 死代码守卫扩到 drive.py+app/god_dialog.py | (3) **修权限洞**: classify 未登记 drive/supervisor/task→默认READ绕过写门禁(测试暴露: drive真实起工作流未被拦)→_COMMAND_LEVEL 补 drive=RUN/supervisor=CLEAN/task=submit RUN+stop clean CLEAN, tests/test_permission(+drive/task/supervisor分类)+test_cli(drive门禁) | (4) CLI测试补 drive 门禁
- [REVIEW] P2#5 T8+权限+守卫 | 1 finding(权限洞,已修) | blockers: 0 | warnings: 0 | 最重要: 权限classify漏drive/supervisor/task→写操作绕门禁(测试test_drive门禁暴露真实drive未被拦), 已补登记为RUN/CLEAN; T8会话深交互/回收写门禁; 死代码守卫扩drive/god_dialog; 275全绿 | 说明: 遗留`regime events --follow`后台进程为早前session遗留(无害tail), 未动
- [REFLECT] 2026-08-08 | progress: 交接前收口——测试架构闭环(T-A E2E系统化 + T-B god专用容器 + T-C god A路HTTP驱动打通 + T-E交接收口)；修regime-god插件3真bug + validate深检skill门控；技术债G1-G14全清 + M0系统化收编全完成；测试基线255 | risk: 真实E2E纠正阶梯(卡死恢复)与元分析接模型未系统验证(记入HANDOVER优先级表); --perm进程级不可绕过为固有边界已如实记录 | next: 书写HANDOVER交接文档(优先级表+命令速查+当前状态), 等待下一session按P0(#1一键入口+#2真实E2E/卡死恢复)推进 | escalate: no
- [REFLECT] 2026-08-05 | progress: 分支求值/工具健壮性(+2)、状态机配置校验(+4)、meta recent_events接入、ROTATE端到端实跑 decision=rotate | risk: 无 blocker | next: 收尾质量门+汇报 | escalate: no
- [REFLECT] 2026-08-05 | progress: 架构方向研究(宪法层→对等多状态机,监督控制理论) + 阶段1(statechart原始)+阶段2(线程运行时)+阶段3(宪法单元能力等价) 全部零回归 146测试 | risk: 阶段4 接入真实 driver/替换 monitor 属侵入性生产改动 | next: 汇报阶段1-3,确认阶段4方案与风险 | escalate: no(方案已获用户确认,阶段4为侵入性集成,先汇报再动)
- [REFLECT] 2026-08-05 | progress: 阶段4a(根不变量运行时强制)+4b(用户自定义宪法可覆写)+4c(宪法信号链E2E真实abort) 157测试零回归 | risk: 技术发现 POST /message 同步阻塞(等模型首条完整回复)，'线程永不阻塞'需发送线程池; 真正把 driver 重构进状态机网络是最后侵入性集成 | next: 汇报技术发现+剩余集成方案,待用户决定是否做最后集成 | escalate: no
- [REFLECT] 2026-08-06 | progress: P1 排查E2E卡顿——完善probe(probe_node_timing.py 全流程节点耗时剖析) + 定位并修复 judge 陈旧文本重发派缺陷(_last_judged_key 只处理一次每回复) 169测试零回归 | risk: agent 节点 time.completed 早于 POST 返回 11s(中间消息/工具调用待查)；probe 用简化 prompt 未复现复杂 prompt 长推理(假设①) | next: 汇报 findings + 剩余假设①复杂 prompt 长推理是否需真实 E2E 复现 | escalate: no
- [REFLECT] 2026-08-06 | progress: P1 mock 基础——MockClient 同接口 drop-in(默认行为+规则匹配+delay/stall/error 故障注入+消息累积)，离线驱动 WorkflowUnit/StatechartDriver 5/5 通过，176 测试 | risk: 现有测试内零散 FakeClient 未收敛(mock 可兼容但未替换)；agent time.completed 早 11s 待查 | next: 进入 E2E 调试(用户指定重点)——用 probe+真实 worker 复现 E2E 卡顿，核实复杂 judge prompt 长推理假设① | escalate: no
- [REVIEW] E2E修复 | 1 issue | blockers: 0 | warnings: 1 | workflow_unit._dispatch序列化正确(每workflow独立_active_dispatch, 多workflow并发保留; await期间drain信号保持STOP响应, abort提前返回); 理论悬案: 若前一POST永不返回且未设heartbeat_stale_sec, await会等(但STOP信号仍可中断; 实测POST均返回, 仅滞后) | 已验证: 回归测试无修复失败, E2E两次COMPLETE
- [REFLECT] 2026-08-06 | progress: E2E 调试定位并修复根因——发派线程池饱和(streaming POST 晚于 message.completed 返回→前 node POST 占线程→judge 发派永久排队→宪法误判 stall)，_dispatch 改 await 前一 POST future；真实 E2E 两次 COMPLETE(60.6s/95.8s)；judge 长推理 21-60s 确认为假设①(非永久卡)；177 测试 | risk: 静态缺陷证明(回归测试无修复 max_active=2 失败)；stall_sec=120 对 60s 长推理有裕量；批次内 POST 序列化略增每 node 墙钟(等真实 POST 返回) | next: 汇报 E2E 根因+修复，确认是否继续打磨(如 CLI 多workflow/收尾) | escalate: no
- [REVIEW] 上帝对话框 | 5 issues | blockers: 0 | warnings: 2 | ① _is_monitor/_is_events 有未用raw参数(nit) ② _run_talk 固定120s deadline+硬编码developer agent(nit) | 其余正确: 事件订阅/命令路由/非阻塞LLM/emit防自我订阅重复/权限门控默认只读(自由文本LLM无法触发写op, 无注入/无密钥泄露) | 已验证: 192测试全绿 + 真实LLM解释E2E + 离线5/5
- [REFLECT] 2026-08-06 | progress: 上帝对话框全自动推进——需求分析+方案定案(对话框应在状态机体系内=是)+ GodDialogUnit对等状态机单元 + REPL(regime dialog) + 能力: 监控/启动/查看/设计workflow/talk独立session交互/LLM解释/权限门控; 192测试全绿 | risk: 无 blocker; 现为无人值守自主推进(用户授权"全自动全方位"); 对话框为MVP, 深度交互/回收/细粒度权限未做 | next: 汇报全貌+方案定案, 待用户确认是否继续对接真实E2E运行或收尾 | escalate: no
- [REFLECT] 2026-08-07 | progress: 推进HANDOVER §8主线——T3非阻塞作业管理(jobs.py注册表+run/run-many --async+job list/status)、T4插件job工具+手册、T5细粒度权限策略(--perm read<interact<run<clean统一门禁)、T7文档同步(opencode载体版)；T6评估后决定不转FakeClient(MockClient非单测fakes的drop-in, 防行为漂移)；T1/T2交互验证需交互环境(本环境opencode run挂起) | risk: jobs注册表并发lost-update竞态(单agent可接受, 已记KNOWN_LIMITS); --perm为操作者自限策略门非授权边界(已注明) | next: 质量门+code-review+汇报; 待用户确认是否做T1/T2交互验证或T8 B路演进 | escalate: no
- [DONE] WORK_PLAN4 R-C: 模板化报告 | verified: 228 passed | cli report 增 --template milestone|blocker|period|activity + --since | E2E: milestone 10条(advance/outcome/reviewer_verdict/transition), blocker 0, period by_outcome complete=5 | journal 读取由 report --history/--template activity 覆盖(不另设 regime journal 命令避免冗余)
- [DONE] WORK_PLAN4 R-A+R-B: Reporter 摄入层 + rollup + regime report | verified: 228 passed(221+7) | 新增 app/reporter.py(ReportRecord归属键schema/ts/project/wf/session/sm/kind/node/phase/outcome + Reporter: ingest/ingest_worker_event/rollup O(1)计数器/journal_slice(append-only JSONL)+load重放) + WorkflowUnit/StatechartDriver 接 reporter + cli run --reporter + `regime report`(--journal --wf --history --limit --json) | E2E: 离线跑写入journal→report读到 outcome=complete entered=6 done=4 | 修: WorkflowUnit新增reporter参数导致cluster位置参数错位→改关键字传参
- [DONE] WORK_PLAN4 E1: SSE 事件流摄入 + 客户端补齐 | verified: 221 passed(218+3) | infra/opencode.py 增 event_stream()(GET /event SSE 解析: event/data 行→{event,data}; 首事件server.connected) + prompt_async/todo/children/fork/summarize | 测试: SSE解析(monkeypatch urlopen) + extras
- [DONE] WORK_PLAN4 I2: --preflight 离线试跑 | verified: 218 passed(214+4) | 新增 app/preflight.py(preflight(): MockClient+StatechartDriver 离线跑全流程判COMPLETE; fault=stall|delay弹性试检) + cli `preflight`命令(--fault/--json) + run --preflight先试跑 | 真实CLI: clean→complete(exit 0), fault stall→blocked(exit 1)
- [DONE] WORK_PLAN4 I1: validate --deep 静态深检 | verified: 214 passed(207+7) | 新增 core/validate.py(deep_validate: role注册/skill可加载/tool注册/节点可达(死岛)/next脊柱环) + cli validate --deep(--skills-dir) | 真实regime.json --deep 通过(ok=true, 0 err/0 warn); 发现 skill_loader.DEFAULT_SKILLS_DIR 默认路径偏一档(需显式--skills-dir, 正常运行已显式传) | 记nit
- [DONE] 治理G6-A修正: supervisor真接线(消死阶梯) | verified: 255 passed | review发现supervisor阶梯/ingest_events曾为纯函数未接线(半接通!) → 重写run()为真看门狗循环(每轮ingest_events+T1健康/docker重启+T2停滞→阶梯执行真实动作abort/restart/human+deadline) + 修SessionWatch首查假停滞(last_message_ts=0→establish baseline) + 死代码守卫扩到supervisor/task/reporter/preflight + 守卫修同文件内部调用也算消费者 | 教训: 不再造"纯函数未接线"的壳能力
- [REFLECT] 2026-08-07 | progress: 技术债治理大推进——C3保障强制 + G9/G11删死代码 + G5权限强制(含修PermissionLevel比较符) + G8修静默兜底(watchdog失明等) + B1修skill路径 + G1唯一归属+sm_id + G10单点真理 + G13插件注入修复 + G14死代码守卫+CLI测试 + G12文档收口 + G6系统化收编(supervisor/task建好真接线、M0代码级删除) | 255测试全绿 | risk: G6-C部署级(退役运行容器删supervisor/stall-watchdog/policy)待执行, 须新supervisor真实验证后做; --perm ceiling默认clean仍宽松(本地CLI无法真不可绕过, env可设clean, 已诚实记录); task.stop/clean已加固 | next: G6-C部署级(退役容器)+真实进程外E2E+更新TECH_DEBT/HANDOVER | escalate: no
- [DONE] T-B+T-C: 专用god容器 + god A路 HTTP驱动E2E打通 | verified: 255 passed + 真实god会话 | 建 docker/Dockerfile.god(基于worker镜像装regime-driver + god.md + regime-god插件, 非--pure, 端口4098) + docker/god-config/opencode.json + .dockerignore; `opencode-god`容器运行(--network host, DEEPSEEK_API_KEY) | god A路HTTP驱动打通: HTTP建god会话→god调用regime_status插件工具→返回真实worker健康JSON→god结构化中文报告(绕开交互TUI!) | **根因修复(regime-god.js)**: ①全可选工具被null args调用崩溃→A(args)null安全 ②conda run输出在工具子进程丢失→直接调env的regime二进制 ③`.text()`不捕获→`await proc.text()` | 暴露并修: validate --deep默认on在容器内因skills-dir缺失硬失败→skill检查仅当显式--skills-dir(否则跳过, 更正确)
- [DONE] 治理G6-B: 建regime_driver.task收编oc-task(消双写derive) | verified: 253 passed | 新增src/regime_driver/task.py(TaskRegistry: submit/list/status/stop/logs/clean + 单一derive) + `regime task` CLI子命令 + infra/oc_tasks.py改薄代理(单一derive) + tests/test_task.py(替test_oc_tasks) | 单真源: report --tasks-dir 走TaskRegistry.list
- [DONE] 治理G14 + G6-A: 死代码守卫 + CLI命令级测试 + 建supervisor收编 | verified: 253 passed(236+17) | G14: tests/test_deadcode.py(扫描src公共方法无生产消费者即fail, 防壳能力复发) + tests/test_cli.py(validate/preflight/report/权限门 6命令级测试) + 删死client extras(prompt_async/todo/children/fork/summarize) | G6-A: 新增src/regime_driver/supervisor.py(进程外: T1健康/T2停滞/deadline/L1-L5纠正阶梯/gate_meta纯函数/choose_action防重放 + ingest_events消费SSE event_stream→Reporter, 接死链) + `regime supervisor` CLI; 设计见DESIGN-supervision.md
- [DONE] 治理G8+B1: 修静默兜底 + 修skill_loader默认路径 | verified: 236 passed(修复13失败) | G8: _report_to_constitution不再静默丢REPORT(必发+带report_error审计,防watchdog失明) + _apply_transition决策fail-fast(rotate仍best-effort日志) + reviewer skill缺失改抛错(不再占位降级) + self_assess._usage不再吞token错为0 + cli恒真try/except删除 + signal handler错误加审计日志(runtime用logging) | B1: DEFAULT_SKILLS_DIR parents[2]→parents[3]真修复(默认路径正确, validate --deep无需--skills-dir; 也解开reviewer fail-fast导致的13失败)
- [DONE] 治理G5: 权限门禁升级为强制(不可自提权) + 修dialog权限提升 | verified: 236 passed(234+2) | Settings.permission_ceiling(默认clean, env REGIME_PERMISSION_CEILING) + permission.clamp(held≤ceiling, 自声明--perm不能超过配置上限) + _gate用clamp后held; dialog allow_write改为由有效held(≥run)派生(非无条件True) | 修: PermissionLevel比较符因继承str而`<=`用字典序→统一为_ORDER比较(潜在bug); 真实CLI: ceiling=read+--perm clean→run/sessions--clean均拒绝(此前竟真实aborted 55 sessions, 现已拦截); 附settings字段同步config.example.toml待补
- [DONE] 治理G9+G11: 删除死代码/双通道残留 | verified: 234 passed(238-4) | 删 reviewer.judge()(阻塞式死双通道, 测试改走生产路径prompt_for/parse_reply) + json_utils.latest_assistant_text(死) + app/telemetry.py+test_telemetry.py+ops/demo_cluster.py(Telemetry生产零消费者) + 死脚本(mock_feasibility/e2e_debug/god_dialog/probe_*/fake_*); blackboard注释更新; 三个"取最新assistant"helper语义确有差异(agent完成/文本过滤/message对象), 强制合并会漂移watchdog/judge路径→**有意保留并记录理由**(非tricky, 属§八"语义不同非重复")
- [DONE] 治理C3+G2+G3: 保障默认强制 + reporter接线 | verified: 238 passed | run/run-many 预检默认强制(--no-preflight可关; async在后台子进程预检不阻塞), validate --deep默认开(--no-deep), run --async补转发--reporter/--no-preflight, run-many接reporter(StatechartCluster加reporter参数, 收尾close) | 依用户拍板方向
- [REFLECT] 2026-08-07 | progress: WORK_PLAN4 第二轮无人值守——R-R3 oc-task收敛(report --tasks-dir 宏观看板并轨) + 保留策略(Reporter.retain + report --prune) + R-C补强(--trace因果链/单对象视图) + E1补强(SSE断流重连退避); 238测试全绿; code-review修复2警告(SSE drop路径retry/backoff, retain重开handle) | risk: R-D深合并(telemetry/blackboard/run-ledger统一真源)未做; SSE摄入未接入运行中driver(事件实时推送链路待接) | next: 汇报; 剩余R-D深合并 + E2 | escalate: no
- [REFLECT] 2026-08-07 | progress: WORK_PLAN4 无人值守推进——I1 validate --deep(role/skill/tool/可达/环) + I2 preflight(离线试跑+fault) + E1 SSE event_stream+extras + R-A/R-B Reporter(归属键+rollup+journal)+run --reporter + R-C report模板化(milestone/blocker/period/activity); 229测试全绿; 每阶段code-review(general) 已修3警告 | risk: 报告bus保留策略(age归档)与统一收敛(oc-task/telemetry/blackboard合并)未做; SSE断流重连未做(单发); R-C报告面缺--trace/单对象视图 | next: 汇报; 剩余R-R3保留策略/收敛 + E2 | escalate: no
- [DONE] 三方向研究定案 + 新规划 WORK_PLAN4.md | ①可用性保障(validate --deep + --preflight 离线试跑) ②事件链路接入——核实 opencode `GET /event` SSE + 插件`event:`hook 可用(push 非轮询), 更正KNOWN_LIMITS旧表述; 仅"进程外独立时钟"缺失归supervisor ③宏观汇报台账——三层 Journal+Report Bus(事件摄入→append-only journal+rollup→模板化regime report/journal), 统一归属键区分wf/session/sm | 已更新 HANDOVER §8 主线+阅读顺序、KNOWN_LIMITS、TASK | next: 按 WORK_PLAN4 优先级实施(P0: I1+I2预检, III R-A+E1 SSE摄入) | escalate: no- [DONE] 发布就绪+工作区模式+DriveClient适配器抽取（2026-08-15，蓝图 `_release_workspace.md` 已并入本日志并删除）| verified: 650 passed（基线624→650，+26）零回归 + general 只读 review APPROVE（0 blocker；W1/W2 warning + N1/N2/N3 nit 全处理）| 交付：①**插件导出形状根治**——`regime-dialog-control.js` 补 `export default { id, server }`（opencode v1 形式；此前纯 named export 在自动扫描路径可能被静默跳过，经 v1.18.11 源码核验）；真源 `.opencode/plugins/` 与 `data/plugins/` 同步，sync --check 绿。②**插件加载验证**——`tests/test_plugin_load.py`（导出形状静态断言 + node --check + 真源/包字节一致 + 无主机路径）+ `scaffold.check_plugin()`（去注释、顺序无关 id/server 检查）+ doctor "dialog-control plugin loadable" 检查项（有部署查部署文件、否则查打包副本）。③**工作区模式（用户核心要求：不污染其它对话环境）**——`scaffold/setup/uninstall --workspace <dir>` 部署到 `<dir>/.opencode/`（`agent/` 单数目录、skills、plugins、package.json、**agent-handbook.md** 随装供用户在 opencode 内读它自助配置）；不写 opencode.json/config.example.toml（不污染项目根）；manifest 落 `.opencode/.regime-deployed.json`，卸载精确移除（用户改过的文件保留，manifest 越界路径防御）；`doctor --workspace` 检查项目级部署；全局模式保留但不推荐。④**DriveClient 适配器协议**——`infra/drive_client.py`（typing.Protocol 13 方法面 + Message 重导出）；内核消费者（app/*、drive、supervisor、parallel、cli helper）类型注解改 DriveClient，构造点保持 OpenCodeClient（**纯接口抽取，运行时零行为变化**，review 逐文件核验）；MockClient 补 4 方法符合协议；`test_drive_client_protocol_conformance` 锁双实现。⑤**版本契约对齐**——`data/opencode-package.json` `@opencode-ai/plugin` `^1.18.0`→`^1.18.11`（与 SUPPORTED_OPCODE=1.18.11 major.minor 一致）+ 守卫测试。⑥**版本 0.3.0 + wheel 重建**（281KB，隔离安装验证：version/DriveClient/templates/plugin 全 OK，无主机路径）。⑦**文档同步**——01_cli（scaffold/setup/uninstall/doctor 工作区+--workspace）、05_setup（工作区推荐）、00_dialog_control、04_distribution_blueprint（wheel 矩阵+用户路径+全局模式）、capabilities、README、howto/host-mode-agents、agent-handbook（真源+data 镜像）、scaffold 模块 docstring 双模式。| 变化前后：基线 624 passed → 650 passed；commit 4eb1f1f + 2efabfd；`dist/` 重建为 0.3.0；测试 +26。| 教训：①必须先改真源再 sync（真源是权威，先改 data/ 会被 sync 覆盖——agent-handbook 曾丢一次改动，已恢复）。②doctor 测试原依赖真实 HOME（开发机有旧全局部署 → 新插件检查正确标红），已改 Path.home 隔离 + 种 key/auth.json 使测试确定性化（这本身证明插件检查工作正常）。③`.opencode/package.json` 是 gitignored 机器本地文件，测试只对 tracked 的 data/ 版断言。
- [REFLECT] 2026-08-15 | progress: 评估"全局装插件+仅主控agent激活"可行性——源码核验 opencode v1.18.11/1.18.15/dev 三线 | 结论：**opencode 无 per-agent 工具门控**（Agent.Info schema 无 tools 字段，dev 分支亦然）；插件工具无条件进全局 ToolRegistry（session/tools.ts `registry.tools({agent})` 的 filter 只看模型类型不看 agent）；`permission.disabled()` 只认全量 `*` deny（无法按工具名隐藏 regime_* 工具，`regime_*: deny` 只在调用时拒绝、不隐藏）；插件启动时加载一次，无 per-session/per-agent 上下文。**"其他 agent 完全不存在"无法实现**（工具对全部 agent 可见）；能做到的是"调用门控"（非主控 agent 调用 regime_* 被 permission 拒绝，需逐 agent 配 `regime_*: deny` 规则——不覆盖用户自定义 agent，不彻底）。工作区模式仍是唯一能实现"完全不存在"的路径（工具根本不注册到其它项目）。| risk: 若用户坚持全局装，可退化为"插件工具对全 agent 可见但 dialog-control 之外调用被拒"——需在文档明示该局限 | next: 给用户两方案对比评估 + 助手脚本"看不见繁琐操作"设计 | escalate: no（纯评估，未改代码）
- [DONE] 发布助手脚本易用性增强+分发验证（2026-08-15 第二阶段，commit aa9bf31 + 78f5c26）| verified: 656 passed（650→656，+6）零回归 | 交付：①**全局模式标注不推荐**——scaffold/setup 全局分支输出缺点说明（opencode 无按 agent 隔离工具机制：源码核验 Agent.Info 无 tools 字段、permission.disabled() 只认全量 `*` deny、插件工具全局注册进 ToolRegistry、无 per-session 上下文）；JSON 输出 `global_not_recommended: true`。②**工作区预检** `precheck_workspace()`——`.opencode/` 已存在 + 用户自有文件检测（manifest 外文件，不覆盖）、路径冲突检测（用户同名 regime-dialog-control.js / dialog-control.md / package.json → 提示"先整理工作区再装"）、git 仓库 `.gitignore` 建议、opencode 运行中检测（`pgrep -x opencode` 精确匹配防路径误报，装完提示重启）；**取部署前状态**（修复：JSON 输出不被部署后 manifest 掩盖用户冲突）。③**真实分发验证 ✅**——真实 opencode 1.18.15 隔离工作区实测：scaffold --workspace 部署的 `.opencode/` 被 opencode 自动发现（`/config` 返回 `plugin: ["file://.../.opencode/plugins/regime-dialog-control.js"]`）、dialog-control/reviewer agent 出现在 `/agent` 列表、`/experimental/tool/ids` 含全部 regime_* 工具、内置 agents（build/plan/general/explore）正常共存；卸载零残留。注：沙箱内 ServeError 为隔离 XDG+npm 伪环境限制（opencode 自动 bun install 需真实环境），真实容器环境（4098 端口 serve）验证通过。④文档同步：05_setup（预检+不推荐原因）、04_blueprint（全局模式不推荐+验证记录）、README、01_cli、capabilities、agent-handbook（真源+data 镜像）。| 变化前后：650→656 passed；commit aa9bf31 + 78f5c26；测试 +6（precheck 4 + global flag 1 + predeploy 1）。| 教训：①JSON 输出若在部署后重算 precheck 会被新写 manifest 掩盖用户冲突——必须在部署前捕获一次并复用；②opencode 插件自动安装（bun install）是异步 forkDetach 的，隔离测试环境需真实 opencode 运行时才能验证插件加载（npm 手动装 SDK 后 import 验证插件形状可作单元级代理）。| review: general 只读 APPROVE（0 blocker；bloker已由78f5c26并发修复；W1目录名.opencode路径不匹配+W2 ok语义+W3 skills冲突 均已修于c1c34c6；657 passed）。
- [DONE] 技术文档全方位同步+版本号统一v0.1（2026-08-15，commit 9dd10d3 + db99aaa + 1bed87f）| verified: 658 passed（657→658，+1 permission 测试）零回归 + mkdocs build 绿 + 全部守卫绿 | 交付：①**版本号统一 v0.1**——`__version__` 0.3.0→0.1.0（唯一真源，pyproject 动态读）；README 中英/docs index "v0.1" 口径；描述符 schema 版本对齐（regime.json/example/flow_v13/flow_spec 文档 0.3→0.1）；wheel 重建为 0.1.0。②**代码注释清理**（workflow 5 批次 28 文件）——移除 WORK_PLAN/阶段号/P0#/W#/F#/R-C 任务代号残留 + 修正 4 断链文档指针（DESIGN-supervision→subsystems/04、DESIGN-drive→subsystems/01、DESIGN-mock→subsystems/08、ARCHITECTURE-statechart-network→architecture/02）+ 历史叙述改现在时；保留 T1/T2/L1-L5/I1/I2/I3 设计术语。③**三个并行审计**（用户文档/架构子系统/代码注释）驱动修复——P0 事实错误（README e2e 路径 tests/→e2e_tests/、README.en docker-in-wheel 错误声明、max_reviewer_retries 默认 2→3、verify_enabled 默认 true→false、config.example.toml 断链）；P1 CLI 缺参补齐（run --title/--task-control-dir、preflight --stall-sec、status --deep 表、report --history/--limit/--tasks-dir/--max-age/--max-records、dialog --base）；P2 红线清理（workflow 4 批次 18 文档：WORK_PLAN/日期戳/历史叙述/完成标记/冻结测试数）；P3 一致性（permission.py `_COMMAND_LEVEL` 补 drive-many RUN/chaos CLEAN/web READ——此前回退 READ 是真实门禁缺口 + 测试；04_permissions 命令表对齐；02_configuration env 表补 REGIME_HOOKS/REGIME_DOCKER_MIRROR/REGIME_WORKER_BASE/REGIME_BIN）。④stale-wrong 修复（ESCALATE 幽灵动作→nudge/interrupt/resume/fallback/kill、capabilities 6→7 子面、03_boundary 角色注册已实现、08_mock DriveClient 协议、分发验证去日期戳、ARCHITECTURE/SUBSYSTEM_DESIGN 索引补 04/10）。| 变化前后：657→658 passed；commit 9dd10d3/db99aaa/1bed87f；wheel 0.1.0。| 教训：①版本号存在"包版本"与"描述符 schema 版本"两个独立维度——统一口径时须分别处理（描述符 version 字段对齐包版本避免用户混淆，代码生成器 `0.design` 保留语义）；②文档审计用三个并行 subagent（用户/架构/注释）分面覆盖效率高，发现面互补；③permission `_COMMAND_LEVEL` 未登记的命令静默回退 READ 是真实门禁缺口——新命令必须登记并测试。
- [DONE] 里程碑收口：general review APPROVE + 注释审计二轮清尾（2026-08-15，收尾 commit）| verified: 658 passed（658→658）零回归 + 全树任务代号扫描零残留 + sync_templates 绿 | review: general 只读 **APPROVE（0 blocker / 0 warning，1 nit N1）**——版本一致性 PASS（__version__ 0.1.0 单一真源、描述符 0.1、README/docs v0.1、无 0.2.0/0.3.0）、注释清理 PASS（T1/T2/L1-L5/I1-I3 设计术语保留）、permission 登记 PASS（drive-many 收紧为真实门禁变化，web/chaos 为声明性登记）、红线清理/CLI 参数/任务代号 grep 全 PASS；N1 已修（02_statechart_network 标题 v1.1/v1.2 版本戳移除）| 注释审计二轮清尾（首轮审计报告晚到，覆盖首轮漏网）：①data/ 与真源 `.opencode/`/根 config.example.toml 任务代号清零（WORK_PLAN6/10/11/13、阶段 2/4、phase-1d/4、W4）——真源改 + sync 派生；②断链指针修正（core/statechart.py→docs/architecture/02_statechart_network.md、config.example.toml DESIGN-worker-isolation→subsystems/02_worker_isolation、DESIGN-usability→guide/04_environment）；③历史叙述/日期戳去除（worker.py "Architecture pivot 2026-08-09"、session_cleanup "verified 2026-08-12"、verify_spec/state_machine "2026-08-14 nightly sg docker" 叙述改功能表述、json_utils "2026-08-15 nightly"、repetition "C3 tuning pass"）；④opencode.py 模块 docstring 补 DriveClient 协议 + Message 重导出声明（审计 NIT 19）。| 教训：①注释审计范围必须含 data/ 与真源模板（首轮审计只扫 .py，漏网全在 shipped 用户可见模板）；②先改真源再 sync 是铁律，data/ 派生改动会被覆盖；③历史叙述改功能表述时保留"为什么"（如 verify 白名单拒绝时机），只删事件性叙述。| next: V-2 PyPI 发布（待用户 token）。
- [REFLECT] 2026-08-15(晚) | progress: **adaptor 层收尾 + 夜间长跑启动**（commit 074d60c + b0713bb，663 passed 零回归）| Phase A：①DriveClient seam 完全化——`infra/drive_client.py` 补再导出 `OpenCodeError`/`is_abort_error`（内核 self_assess/workflow_unit 改从 seam 导入，运行时零变化）；②新增 `tests/test_adaptor_seam.py` 边界守卫（内核 app/core/drive/supervisor/parallel/task/flow/regime 禁直连 infra/opencode，构造点白名单 dialog_app/parallel）+ `docs/subsystems/01_drive.md` 新增 DriveClient seam 章节；③**夜间前置暴露两个真实缺陷**：a) preflight 30s 硬上限——11 节点流程离线试跑需 ~90s，drive 强制 preflight 必败；修 `_scale_timeout(node_count)=max(30,8n)` 默认自动缩放，CLI 5 处硬编码移除；b) MockClient `_NODE_RE=\w+` 把 'test-core' 截成 'test' → 'no node test' 循环 dispatch_error 挂死；改 `[A-Za-z0-9_-]+`（非模板 flow 命名方案可用）；标准 6 节点模板无连字符所以从未暴露 | Phase B：**夜间长跑设计**——用户要求"尽可能长且复杂 + 独立 opencode 主控实例 + 从安装 regime-driver 全流程 + 我不当主控 + 定时检查 + 跑完分析"。实现：night-run-20260816/（.gitignore）——conda python 建 `--system-site-packages` venv 装 wheel 0.1.0（pip --no-index --no-deps）→ `scaffold --workspace ws` → 写 ws/.opencode/opencode.json（补 developer agent，docker worker 同款配置；workspace 模式此前缺 developer 是真实缺口）→ 独立 `opencode serve --port 4297`（隔离 XDG_CONFIG/DATA/CACHE 全落工作区，绕开沙箱禁止的 ~/.local/share/opencode/log 写与旧实例占用；不动旧 4097/4098/4199 实例）→ 主控窗口会话（dialog-control agent 自主上岗检查：worker 健康/会话/流程注册表，回复"主控窗口就绪"）→ 11 节点流程 flow-night-mega.json（dqueue 分布式任务队列：understand/design 门/impl-core/test-core 门/impl-pool/test-pool 门/impl-store/test-store 门/impl-api/test-api 门/wrap，watchdog soft 180/hard 1800）→ drive 启动（preflight complete → 真实运行：understand→design 门通过→impl-core busy）| 监控：monitor.sh 每 5 分钟快照（health/会话/日志），修复了"preflight outcome 记录触发 RUN_FINISHED 假阳性"的 grep | 环境要点：docker 沙箱禁用（setgroups denied）→ host-mode；沙箱禁写 ~/.regime/tasks → drive --tasks-dir 指向工作区；/tmp 跨调用清空 → 全部产物落 night-run-20260816/ | 教训：①"尽可能长和复杂"的流程正是暴露模板掩盖缺陷的试金石（preflight 30s/节点 id 连字符都是 6 节点模板测不出的）；②MockClient 是 DriveClient seam 的测试实现，其解析假设（节点 id 格式）必须与协议一致，否则离线试跑与真实运行行为分叉；③workspace 模式缺 developer agent 是文档/脚手架未覆盖的接缝——本轮以 opencode.json 补上，长期应入 scaffold 交付物 | next: 长跑完成后流程/日志/代码质量分析 | escalate: no
- [DONE] 夜间长跑完成：dqueue 11 节点全流程 **complete @ wrap（2191.2s ≈ 36.5min）**（2026-08-16 凌晨，drive task-20260816-001920-6eec）| 流程：understand(1.3min)→design门(advance 0.85)→impl-core(2.1min)→test-core门(advance 0.9)→impl-pool(2.6min)→test-pool门(**issue_pending 0.85 → 返工环 → issue_resolved 0.9**)→impl-store(6.8min 最长)→test-store门(advance 0.88, 11min 深审)→impl-api(2.7min)→test-api门(advance 0.88)→wrap(1.6min) | 质量证据：①**审查闭环实证**——test-pool 审查抓到真实生产级缺陷（backoff_delay 0.1*2**attempts 无上界：attempts=1024 时 OverflowError 在 except 块内杀死 worker 线程；attempts 60+ 退避 10^17s 永久挂死）→ blocking 质询 5 点 → 开发者实质修复（MAX_BACKOFF=30 + 先封指数 _MAX_BACKOFF_ATTEMPTS=9，比建议更优）+ 3 新用例 → 重审 issue_resolved；②**产物宿主独立复验**：dqueue 2303 行（task_queue 145/worker 111/store 243/api 191+183/tests 1400 行 87 用例）**全量 87 passed**（21 core + 19 pool + store + api），compileall 全绿，README 完整；③**零异常**：无 dispatch_error/无 stall/无 watchdog 阶梯（监控 15 次快照 health 恒 200）；④**上下文协商** 3 次（impl-store/test-api/wrap）均未阻塞；⑤**主控窗口**（独立 opencode dialog-control agent）全程自主待命：上岗检查 + 中途状态汇报（进程/节点/判定置信度）+ 最终评估；⑥**adaptor 层验证**：整个 36.5min 真实运行全部经 DriveClient seam 驱动（构造点 OpenCodeClient 连 4297），协议 13 方法面全走通 | 前置修复价值复证：preflight 缩放 + MockClient 连字符 id 是本次长跑能启动的前提 | 环境要点：host-mode（docker 沙箱禁用）；独立 XDG 隔离（4297）；产物 night-run-20260816/（gitignored，分析后归档摘要入 docs）| next: 主控最终评估收口 → HANDOVER 更新 → 汇报 | escalate: no
- [DONE] 硬编码超时全面审计 + fail-safe 化（2026-08-16，commit 待）| verified: 668 passed（663→668，+5 守卫）零回归 | **用户质询**："preflight 30s 硬编码门限，怀疑更多类似隐患，不应允许可能误杀任务的硬编码，参考 DSH 更成熟设计" | **全量审计**（settings/opencode/workflow/supervisor/driver/cluster/dialog/reviewer 全部 timeout/上限/重试）：🔴 两个"墙钟杀任务"默认值——①`default_deadline_sec=600`（每阶段墙钟：busy 但流式推进的节点超 10min 即 abort 会话+TIMEOUT；与 SSE 活性看门狗职责重复且更粗暴，夜间长跑 impl-store 408s 险些触发、test-store 11min 靠判定轮次重置计时幸存）；②`max_driver_wait_sec=3600`（无 --deadline 时整跑 1h 上限，多小时健康长跑必死）；🟡 类默认与 settings 单一真源背离——`Reviewer.max_retries=2` vs settings 3、`OpenCodeClient.timeout=240` vs settings 600（裸构造路径会砍长生成 POST）；🟢 良性/设计内——stall_sec=120（活性判定，默认开）、max_total_nodes=50（反失控可配）、human_confirm 300（可配）、docker/健康探测 5-60s、poll/sleep 循环 | **修复（DSH 原则参照）**：DSH 的成熟做法=超时全配置化（preset YAML timeoutMs）、阻塞有界+后台无超时、默认显式 fail-safe；据此——①`default_deadline_sec` 默认 600→**None（禁用）**，墙钟杀开关改为显式 opt-in（--deadline/config），停滞兜底交还 SSE 活性看门狗；②`max_driver_wait_sec` 默认 3600→**None（无墙钟上限）**，反失控由看门狗+max_total_nodes 负责；③`Reviewer.max_retries` 2→3、`OpenCodeClient.timeout` 240→600（对齐 settings 单一真源）；④driver.run/cluster.wait 支持 deadline=None 语义；⑤新增 `tests/test_no_silent_kill.py` 守卫（墙钟杀手默认关、活性看门狗默认开、POST 超时宽裕、类默认与 settings 对齐、反失控上限可配）+ `docs/reference/02_configuration.md` 新增「超时与误杀防护」语义表 | 变化前后：663→668 passed；测试 +5 | 教训：①"防挂死"与"杀任务"是同一枚硬币——墙钟超时只能防"无活性挂死"，而活性判定（SSE）才是区分"卡死"与"慢但健康"的唯一正确信号，墙钟默认必须 fail-safe；②类级默认值一旦与 settings 分叉就是潜伏的裸构造陷阱（reviewer/opencode 裸构造路径行为与配置路径不一致）；③夜间长跑是审计的催化剂：标准 6 节点流程永远跑不出 10min 单阶段，暴露不了这些默认值 | next: 汇报 + V-2 待用户 | escalate: no
- [DONE] 超时默认值合理化 + 超时尽力恢复 + 易用性补缺（2026-08-16，commit 待）| verified: 670 passed（668→670，+2）零回归 | 用户指示："超时配置即便配置化也应有更合理默认值；超时后应尽力恢复；参考 DSH 配置" | 交付：①**更合理默认值**——`stall_sec` 120→180（长思考/突发流式裕量，对齐夜间长跑实测 soft 180 经验；config.example + 文档同步）；②**每阶段墙钟尽力恢复**——到期先查会话状态：仍 busy（服务端在产出）→ `deadline_grace` 宽限续期不杀任务，仅 idle（真卡死）才 abort；真停滞仍归 SSE 活性看门狗（workflow_unit._session_still_busy + 测试）；③**preflight 超时尽力恢复**——离线试跑墙钟到期自动 2× 预算重试一次，仍失败才诚实失败（monkeypatch 测试）；④**易用性补缺**——`docker/worker-config/agents/developer.md` 新增（真源+sync），workspace/global 部署开箱即含 developer 执行 worker（夜间长跑暴露：workspace 模式此前缺 developer agent，需手写 opencode.json）；scaffold 测试补 developer 断言；⑤文档——02_configuration「超时后的尽力恢复」三机制（POST 重试+轮询兜底/墙钟 busy 宽限/preflight 2× 重试） | 变化前后：668→670；测试 +2 | 教训：①"超时后尽力恢复"的正确判据是会话活性（busy=还在工作→宽限，idle=卡死→可杀），墙钟只对 idle 有意义；②DSH 的 timeoutMs=300000 佐证"默认给足裕量、kill 是最后手段"；③scaffold 文档自称带 developer/reviewer 模板但 data/agents 只有 reviewer——文档与交付物漂移靠夜间实战暴露 | next: 汇报 | escalate: no
- [DONE] 易用性/稳健性/可观测性自主推进轮（2026-08-16，commit 待）| verified: 676 passed（670→676，+6）零回归 | 用户授权按建议自主推进，强调"稳健性、可检查可监控、监控体系背后的易用性直观性" | 交付：①**崩溃续跑 `--resume <journal>`**——新 app/resume.py 重放 reporter journal 定位第一个"已进入未完成"节点（node_enter 无 node_done），drive 从该节点续跑（先前节点跳过、会话全新、磁盘产物保留）；journal 空/complete 时拒绝启动；async argv 透传；文档 01_cli | ②**drive 自带周期监控快照**——`--monitor <path>` + `--monitor-interval`（默认 60s）：独立线程写 JSONL（ts/uptime/node/phase/sessions/busy），首行无条件写（快跑也有轨迹）；踩坑：drive.py 缺 `import json`（NameError 被静默吞掉→空文件，插桩定位） | ③**drive 结束 notable 摘要**——DriveResult.notable 从账本计数恢复/异常事件（dispatch_error/context_handover_error/deadline_grace/watchdog_fire/monitor_abort/workflow_nudged），CLI 结果行打印；`{outcome,...,notable}` 入 JSON | ④**doctor 角色 agent 完整性**——部署目录含 developer.md/reviewer.md（离线确定性）+ worker /agent 列表含 developer/reviewer（在线）；缺失即标红（任何 flow 派发必败） | ⑤**talk_timeout 可配置化**——`dialog_talk_timeout_sec`（settings 默认 120，config.example+同步+doc 守卫），DialogControlUnit 参数化 | ⑥文档：01_cli（--resume/--monitor/--monitor-interval/notable/doctor 检查项/--stall 默认 180）、02_configuration（dialog_talk_timeout_sec） | 变化前后：670→676；测试 +6（resume 4 + monitor 1 + notable 1）；wheel 重建 | 教训：①吞异常的最隐蔽形态=NameError 被 except Exception: pass 吃掉（监控线程静默空文件），调试要插桩 except 而非猜；②"可检查可监控"要落在产物上——monitor 文件/notable 摘要/doctor 检查项三者构成"运行中可 tail、结束后可摘要、部署前可体检"的闭环 | next: 汇报 | escalate: no
- [DONE] 全方位文档体检 + 四文档制清理（2026-08-16，commit 待）| verified: 676 passed 零回归 + mkdocs 绿 + 死链 0 + 守卫全绿 | 流程：按 doc-governance skill 9 阶段——尺子=WRITING_GUIDE E 红线 + G.4；4 个并行 subagent 全覆盖审计（guide+howto / reference+arch+subsystems / README+capabilities+KNOWN_LIMITS / 体系结构）| **临时文档清理（用户指示：保留四类主要任务控制文档）**：删除 tasks_docs/{quality_report,durability_report,quality_deep_check,release_readiness_audit}.md + {nightly,quality,durability}_run_archive/（~28MB）+ night-run-20260816/；结论均已在 WORKLOG 条目；HANDOVER 加数据地图清理说明；PENDING_TASKS 修正过时 0.2.0→0.1.0 | **审计发现与修复**：P0×4——①config.example.toml 未注释的 max_driver_wait_sec=3600/default_deadline_sec=600 与 Settings 默认 None 矛盾（复制示例即启用墙钟杀手）→ 注释化+说明；②02_configuration 语义表 stall_sec=120→180 + 两行历史叙述（"曾默认…已改"）改纯当前语义；③KNOWN_LIMITS stall 120→180 + "两级 watchdog 协调边界"条目重写（外部 supervisor 在 drive 模式 supervise_sessions=False，双头竞态已消除——原条目描述过时行为）；④07_release 两处指向已删临时文档的死链→改指 WORKLOG | P1×16：07_release 5 处完成/历史标记；09_testing_architecture 进度表/完成标记/过程叙述/本 session 语境全清（改为"当前能力"现状描述）；03_parallel/02_worker_isolation/05_chaos/08_mock 的 ✅ 核查清单+冻结计数（119.6s/（10）/（4）/16→96/231MB/3.4MB/17 files）全去；CLI_REFERENCE `--base :4097` 缺 scheme 错误写法；05_setup/host-mode-agents 部署清单漏 developer.md（先于审计已修+本审计补齐 doctor 描述）；04_distribution "setup（规划）"→setup + pip 表补 developer.md；02_statechart "替代固定 240s"历史叙述；--stall help 文案 120→180（CLI 代码）| A.4 归属收敛：看门狗两套阶梯明确命名（进程内 nudge→interrupt→resume→fallback→kill / 进程外 abort→换模型→重启→人工，01_principles/00_dialog/05_chaos 对齐 04_supervisor 权威）+ 05_contract 权限表去重指向 04_permissions + 01_drive 13→14 方法面 + 构造点白名单表述补全 | **Phase 5 交叉核验**：红线模式全扫 0 残留、死链复扫 0、stall/13 方法/冻结数字残留 0 | 教训：①文档"唯一真源"声明（config.example）必须与真值一致——示例文件里的未注释旧默认比文档更危险（用户直接复制）；②审计并行分面覆盖互补（README 审计抓到 07_release 死链、reference 审计抓到 config.example P0、结构审计抓到 A.4 归属）；③删除临时文档后必须同步检查活文档指针（07_release 两处死链即删除的副作用）| next: 汇报 | escalate: no

- [DONE] 文档系统完整审计 + 更新（2026-08-16，commit 待）| verified: 677 passed（676→677，+1 守卫）零回归 + mkdocs build 绿 + 死链 0 + 守卫/sync_templates/check_capabilities 全绿 | 按 doc-governance skill 9 阶段：尺子 = WRITING_GUIDE E 红线 + G.4；4 路并行 subagent 全覆盖（guide+howto / reference+arch+subsystems / 根索引 / 体系结构；reference 路因超长运行时中断，覆盖由其余 3 路 + 本人独立核实补足）| **P0 事实错误修复**：①CONTRIBUTING.md E2E 路径 `tests/test_e2e_worker.py`→`e2e_tests/...` + 冻结数字"333+ tests"删除；②对话框权限默认值三处误述（guide/00"对话框默认只读"、howto/dialog-control"regime dialog 只读"、"默认持 clean"）→ 修正为 CLI `--perm run` 默认写能力 + 程序化构造默认只读；③guide/02 run-many"互不干扰"与 06"共用文件系统可能碰撞"矛盾 → 对齐 06；④howto/dialog-control "flow design 同 design"（对话框内无此子命令）；⑤howto/run-many-sessions `app/statechart_cluster.py` 断链 → 引 subsystems/03；⑥howto/run-e2e `--trace <wf>`（--trace 是 flag）| **P1 红线修复**：guide/07 移除 tasks_docs/HANDOVER/AGENTS 引用 + 冻结"2h 稳定"结论；howto/run-e2e 冻结时序数字软化；WRITING_GUIDE A.5.1 标题去任务代号（WORK_PLAN7 III）| **A.4 归属收敛**：guide/03 hooks.py 完整代码示例（复制自 10_extension_points）→ 压缩为指针；08_mock DriveClient 方法子集 → 引 01_drive seam；07_carrier 可行性量化/路线图 → 现状态表述；architecture/02 全文重写——去"质询/构想/可行性/调研/决策"过程叙事，保留监督控制理论/根不变量/并发选型全部实质（E 红线 + A.6 最大项）| **断链修复**：KNOWN_LIMITS.md 裸引用（05_contract/howto/run-e2e/run-many-sessions）→ docs/ 前缀；howto/dialog-control `agents/dialog-control.md`（单数 agent）；guide/03:137 误链 → 10_extension_points | **文档管理规范层**：MAIN_TASKS 按"更新纪律"移除 8 条已完成主线（历史入 git/WORKLOG）+ 头部日期更新；PENDING_TASKS 清理过时 W1–W6 块（全部已关闭）；**TECH_DEBT.md 删除**（G1–G14 全清后的过时登记，含死链与已失效声明；config.example G5 引用改指 PENDING_TASKS；HANDOVER/WORKLOG 引用改指闭源记录）；系统文档导航/docs/README 对齐 mkdocs nav（补 capabilities/CLI_REFERENCE/ARCHITECTURE/04_distribution/subsystems 各篇 + mkdocs 补"文档地图"）；子系统 10 篇补"深入指引"出链（A.8 读者旅程）| **CLI 代码对齐（审计暴露）**：①`drive --stall` help 文案 "config default 120"→180（与 settings.stall_sec 一致）；②`Supervisor.stall_sec` 裸默认 60→None（回退 settings.stall_sec=180，消除独立 supervisor 硬编码 60s 长思考误杀隐患，与 2026-08-16 硬编码超时审计同源）；CLI `supervisor --stall` 默认同步 None；tests/test_no_silent_kill.py +1 守卫 | **general 只读 review APPROVE（0 blocker）**，warnings 全收口：W1 `ops/quality_run.py` 硬编码 `stall=60`（会把 180 盖回 60）→ 默认 None（不传 `--stall` 走 settings）；W2 supervisor_cmd 用裸 `Settings()` 不读 config/env → help 文案改精确（默认 180）；W3 进程内看门狗硬编码 `stall_sec=120`（`WatchdogUnit.__init__` 默认 + `statechart_cluster` 回退 `or 120.0`，run-many/dialog-B 与 run/drive 的 180 不一致）→ 默认 None + 回退 `Settings().stall_sec`（三处同源收口，全树 `stall_sec` 硬编码清零）；nits 全收（test 补裸构造断言、01_cli/agent-handbook 示例 stall_sec 120→180、01_cli supervisor --stall 补默认）| 变化前后：676→677 passed；41 文件；临时工作簿 `_doc_audit_20260816.md` 完成即删 | 教训：①"人类手册红线"（doc-governance Phase0 禁 skill/agent 引用）与本仓 WRITING_GUIDE（无此禁令）+ 产品概念性 skill 解释实践存在治理张力——本轮保留概念性解释、仅移除纯内部工作流引用（tasks_docs/HANDOVER/AGENTS），完整裁定边界待用户；②文档路径引用三类惯例混用（docs/ 仓库相对 31 处 / reference/ 目录前缀 17 处 / ../ 文件相对 19 处 / 裸文件名 17 处）——已修真断链，剩余上下文可解析者留待后续统一；③审计暴露代码问题（supervisor 60s、看门狗 120s 硬默认、CLI help 文案）属"文档与代码互证"的副产品，同批收口 | next: V-2 PyPI（待用户）| escalate: no

- [DONE] 人类手册红线彻底执行（2026-08-16，commit 待）| verified: 677 passed 零回归 + mkdocs 绿 + 死链 0 + sync_templates/check_capabilities 全绿 | 用户裁定："彻底执行" doc-governance Phase0 人类手册红线——清除 docs/ 用户手册层（guide/ + howto/ + index + README 中英）全部智能体元信息（skill 名、agent 文件引用、subagent、插件/工具配置、agent 指令与工作流过程）| **清除**：guide/00 移除技能注入表列/整节"为什么 skill 不直接加载进对话框"/"为什么 worker 干净对话面装插件"整节/analyst·advisor 助手/对话 agent 措辞（改"对话操作面"）；guide/02 去 skill 注入+解析性表述；guide/03 hooks 插件→指针；guide/05 部署文件清单去 agent 文件细节（agent 定义/subagent/插件名/让 agent 读说明书）；guide/07 去 agent-handbook/agent 视角措辞；howto/dialog-control 去 A 路 agent 指令/文件/插件（保留 A/B 双路用户概念+命令面）；**删除** howto/dialog-control-window.md + host-mode-agents.md（纯 agent 元信息：HTTP 驱动测试过程 + 主机 agent 模板配置）；index/README 中英 去 对话 agent/agent 模板/A 路插件/subagent 名；同步 mkdocs nav + docs/README + howto/README + KNOWN_LIMITS/07_carrier/09_testing 引用（0 死链残留）| **保留例外（明确记录）**：capabilities.md skills 段（check_capabilities.py + test_capabilities_map.py 基础设施强制，删除即挂）、reference/ 的 --skills-dir/skills_dir/REGIME_HOOKS/verify/skill 字段（产品契约 schema 非 agent 指令）、architecture/ + subsystems/（开发者实现文档非人类手册层）、`--assistants` CLI 旗标（产品契约）、index/README 的"skills/插件"部署类别名词 | 变化前后：17 文件改 + 2 文档删除；677 passed 零回归 | 教训：①红线执行需先确认基础设施依赖（capabilities.md skills 段被检查器强制引用，全删会挂——例外必须显式记录）；②删文档要全仓扫引用（HANDOVER 3 处死指针 + KNOWN_LIMITS/07_carrier/09_testing 各 1 处）同步修复；③"改写为人类可读表述"与"移除"的边界：产品概念（控制对话框=对话操作面、A/B 双路、命令面）保留并改写，agent 指令/文件/插件名/技能注入机制 一律移除 | next: 汇总 + push（用户已授权同步）| escalate: no
